# 決定事項・未決事項（Decision Log）

行は削除しない。決定したら「決定済み」へ移し、日付・決定者・内容・根拠文書を書く。
種別: 投資ロジック（ユーザー/ChatGPT 判断必須） / 技術 / 費用 / 運用 / 規約

---

## A. 決定済み

| ID | 論点 | 決定内容 | 決定日 / 決定者 | 根拠 |
|---|---|---|---|---|
| D-00 | v5.1 の Canonical Source | 会話内でユーザーが確定させた v5.1 全文そのものを Canonical とし、`docs/prompts/short-surge-v5.1.original.md` に一字一句変更せず保存、保存時点の SHA-256 を MANIFEST に記録。formatting / normalization / typo correction をしない。**登録そのものは全文の受領待ち** | 2026-09-15 / 監査 0.2 最終 #8 | audit final #8 |
| D-01 | Entry の基準価格 | signal / decision / entry を分離。成績・Threshold は `entry_reference_price`。`actual_fill` は任意。算出方式は D-01a | 2026-09-15 / 監査 0.1 #6 → 0.2 #3 | audit 0.1 #6, 0.2 #3 |
| D-02 | USD/JPY の時点 | `fx_observed_at <= decision_cutoff_at` 必須 | 2026-09-15 / 監査 0.1 #7 | audit 0.1 #7 |
| D-03 | ストレージ設計 | Postgres = 状態・索引・結果、大量の履歴 = Parquet + Object Storage | 2026-09-15 / 監査 0.1 #3 | audit 0.1 #3 |
| D-03a | Supabase のプラン | **Phase 1 開発は Free で開始してよい。** Production Architecture を Free 上限に合わせて縮小しない。Bulk historical data の分離を維持。**Production 開始前にプランを再評価** | 2026-09-15 / 監査 0.2 最終 #7 | audit final #7 |
| D-03d | Supabase Free 上限への対応 | 監査は当初 (c) ローカル Supabase を採用。その後ユーザー指示で `loop-vocabulary` を **Pause（削除しない）** し、空いた枠に本プロジェクト専用の新規 Free Project `surge-research-platform`（ap-northeast-1、$0/月）を作成。`kaiji-radar` は無変更。既存 Project の流用なし。有料化なし。ローカル Supabase は開発・テスト用に併用 | 2026-09-16 / ユーザー指示 | ユーザー指示（2026-09-16） |
| D-05 | Worker 実行環境 | `JobRunner` / `Scheduler` で抽象化 | 2026-09-15 / 監査 0.1 #4 | audit 0.1 #4 |
| D-06 | J-Quants の位置付け | Free = 開発用、Production EOD は Light 以上を候補、Standard は必須にしない。分足・ティックは日次16:30頃更新のため場中 ENTRY 判断に使わない | 2026-09-15 / 監査 0.1 #15、0.2 #2 | audit 0.2 #2、公式更新スケジュール |
| D-07 | 米国株データの選び方 | `MarketDataProvider`、用途別 Provider、公式仕様のみで比較 | 2026-09-15 / 監査 0.1 #14 | audit 0.1 #14 |
| D-08 | 分足の要否 | 全 Universe は日足、Stage 2・Watch・ENTRY 候補のみ分足 | 2026-09-15 / 監査 0.1 #8 | audit 0.1 #8 |
| D-09 | 同一足で両方に触れた場合 | `AMBIGUOUS_PATH` | 2026-09-15 / 監査 0.1 #9 | audit 0.1 #9 |
| D-09a | データ欠損の扱い | 始値 → 日足 → 分足 → 約定 → AMBIGUOUS_PATH。降りられないデータ不足は `UNRESOLVED_MISSING_DATA` | 2026-09-15 / 監査 0.2 #11 | audit 0.2 #11 |
| D-09b | 「データなし」と「欠損」の区別 | 細かいデータが仕様上存在しない → `AMBIGUOUS_PATH`、本来あるはずのデータが欠損 → `UNRESOLVED_MISSING_DATA`（Claude Code 案を採用） | 2026-09-15 / 監査 0.2 最終 #6 | audit final #6 |
| D-10 | Universe の範囲 | `universe-1.0.0` | 2026-09-15 / 監査 0.1 #10 | audit 0.1 #10 |
| D-13 | 教師ラベルの構造 | Objective / Interpretive | 2026-09-15 / 監査 0.1 #11 | audit 0.1 #11 |
| D-17 | 同一銘柄の重複 ENTRY | Prediction Episode | 2026-09-15 / 監査 0.1 #12 | audit 0.1 #12 |
| D-17c | Horizon と Failure Line | ENTRY から 20 trading sessions、リセットしない。`initial_failure_line`（固定・Primary）と `current_risk_line`（研究用） | 2026-09-15 / 監査 0.2 #6・#7 | audit 0.2 #6, #7 |
| D-17d | Horizon の表記 | S0 = ENTRY 成立セッション（ENTRY 時刻以降の S0 の値動きを含む）、S1 = 翌取引セッション、Primary Horizon は S20 close まで（Claude Code 案を採用） | 2026-09-15 / 監査 0.2 最終 #5 | audit final #5 |
| D-17e | THESIS_INVALIDATED 後の Outcome | `primary_episode_outcome`（Episode 終了まで、正式評価）と `counterfactual_horizon_outcome`（当初 S20 close まで、研究用）に分離。THESIS_INVALIDATED 後の +20% 到達を Primary の成功に戻さない。**Claude Code の暫定案（Primary を S20 まで計算）は不採用** | 2026-09-15 / 監査 0.2 最終 #2 | audit final #2 |
| D-18 | ENTRY 時の3,000円再判定 | `decision_price` で再判定 | 2026-09-15 / 監査 0.2 #4 | audit 0.2 #4 |
| D-19 | 引け後に取得した材料 | `TECHNICAL_SETUP_EOD` と `POST_CLOSE_CATALYST_SETUP` を分離 | 2026-09-15 / 監査 0.2 #5 | audit 0.2 #5 |
| D-22 | 指示書・監査の原文 | D-00 と同じ考え方（会話内で確定した本文を Canonical とする）を適用し、既存の `*.original.txt` を正本として扱う。**Claude Code 解釈。監査は v5.1 について明示したもので、指示書・監査への適用に異論があれば再オープンする** | 2026-09-15 / Claude Code（監査 0.2 最終 #8 からの類推） | audit final #8 |
| D-23 | 見逃し判定と backfill | `ACTIONABLE_FALSE_NEGATIVE` / `PIPELINE_MISSED_ACTIONABLE_SIGNAL` / `OUT_OF_SCOPE_SHOCK` の3分類。backfill 情報を当時 AI が知っていたことにしない | 2026-09-15 / 監査 0.2 最終 #3 | audit final #3 |
| D-25 | 米国株の Outcome 通貨 | Eligibility = JPY 換算、Threshold・価格 Outcome = USD | 2026-09-15 / 監査 0.2 #8 | audit 0.2 #8 |
| D-26 | 判断後の entry 価格が3,000円超 | `entry_reference_price` でも再確認し、超過なら Prediction・Episode を作らず `ENTRY_ABORTED_PRICE_LIMIT`（研究ログ）。再び3,000円以下になったら再分析。**Claude Code の暫定案（Prediction は有効）は不採用** | 2026-09-15 / 監査 0.2 最終 #4 | audit final #4 |
| D-27 | 材料の利用可能時刻 | 4つの時刻。Production・Replay は `available_to_model_at <= decision_cutoff_at` のみ | 2026-09-15 / 監査 0.2 #9 | audit 0.2 #9 |
| D-28 | 分割・併合・配当 | raw を保存、Outcome は比較可能な系列、配当は Target に加算しない | 2026-09-15 / 監査 0.2 #10 | audit 0.2 #10 |
| D-29 | 過去急騰高値の役割 | 上昇根拠・Potential Upside への使用は禁止。Resistance / Supply Overhang / Historical obstacle / 高値掴み保有者として保存・評価。**存在するだけで Reachable Zone 上限を機械的・単調に引き下げる DB 制約は設けない**（Supply Overhang の意味は失効・低下しうる） | 2026-09-15 / 監査 0.2 #1 → **0.2 最終 #1 で緩和** | audit 0.2 #1, final #1 |
| D-30 | v5.1 の管理 | Canonical v5.1（immutable original）と post-v5.1 decisions（versioned addenda）を混ぜない | 2026-09-15 / 監査 0.2 #12、最終 #8 | audit 0.2 #12, final #8 |
| D-00 | v5.1 Canonical の登録 | **RESOLVED**: `docs/prompts/short-surge-v5.1.original.md`（32,012 バイト、2,275行、LF、SHA-256 `32bf001e…f877a`）を ChatGPT が直接コミットし（`1a4f34e`）、Claude Code が実ファイルから SHA-256 を実測して MANIFEST に登録。post-v5.1 用語の混入がないことを確認（RF-14b） | 2026-09-16 / ユーザー + ChatGPT | audit 2026-09-16 引継ぎ |
| D-10b（JP 判定ソース） | JP の普通株判定に使うソース | **JPX「東証上場銘柄一覧」（data_j.xlsx）の「市場・商品区分」を採用**（認証不要、市場区分と商品区分が1列）。J-Quants `ProdCat` は不使用。外国株式5件・出資証券2件は引き続き UNRESOLVED | 2026-09-16 / Claude Code（Phase 1 実装） | [universe-definition-v1.0.0.md](specs/universe-definition-v1.0.0.md) |
| D-34 | Security / Issuer の同一性 | **公的レジストリ由来のキーのみを同一性とする**（US = SEC CIK、JP = EDINET コード、Security の JP = JPX ローカルコード）。Ticker と正規化名称は同一性にしない。作れないときは `PROVISIONAL` と明示。`STRONG` キーの衝突は統合せず両方を降格し、run 警告に残す | 2026-09-16 / 監査 Phase 1.1 #1〜#3 | [security-identity.md](specs/security-identity.md) |
| D-35 | 属性変更の履歴 | Ticker・名称・識別子・上場区分/状態は **SCD2**（変化時のみ open 行を閉じて新規行、無変化は `last_confirmed_at` のみ前進、open 行は常に1行）。as-of 読み出しは `ref.listing_states` 経由 | 2026-09-16 / 監査 Phase 1.1 #4・#5 | 同上 |
| D-36 | Object Storage からの一括読み込み | **短命な署名 URL のみ**受け付ける。https 必須、host allowlist 必須、DB へ raw API key を渡さない・DB は Authorization ヘッダを送らない。allowlist は環境設定であり migration では seed しない | 2026-09-16 / 監査 Phase 1.1 #6 | [phase-1-universe-sync.md](runbooks/phase-1-universe-sync.md) |
| D-37 | Production worker の DB principal | 専用の LOGIN role `surge_worker_prod_app`（`surge_worker_prod` のメンバー）で接続する。パスワードは DB 内で生成し Supabase Vault に保管、チャット・ログ・Git に出さない。履歴テーブルへの DELETE と research スキーマへのアクセスを持たない | 2026-09-16 / 監査 Phase 1.1 #7 | 同上 |
| D-38 | 下流工程の取得対象 | **価格・FX の取得対象は `INCLUDED` ∪ `UNRESOLVED`。** `UNRESOLVED` を黙って落とさない（落とすと判定確定前の価格が欠落して後追い評価ができない）。ただし `UNRESOLVED` は ENTRY 候補にしない | 2026-09-16 / 監査 Phase 1.1 #12 | [universe-definition-v1.0.0.md](specs/universe-definition-v1.0.0.md) §5 |
| D-39 | 同一性を変える再構築の手順 | 旧 ID を `ref.identity_migration_map` に記録 → master/判定/coverage/staging を削除（`pipeline.runs`・`source_fetches` は保持）→ 公式ソースから再取得 → 新旧 ID 対応を埋める。手順は versioned migration と worker code に残す | 2026-09-16 / 監査 Phase 1.1 #10 | [security-identity.md](specs/security-identity.md) §5 |
| D-44 | Identity confidence の語彙 | `STRONG`（そのものに対するレジストリ識別子）/ `REGISTRY_ANCHORED`（レジストリ識別子＋provider テキスト由来の属性）/ `PROVISIONAL` の3段階。US の `US:CIK:<cik>:<type>:<class>` は表示名由来の要素を含むため `REGISTRY_ANCHORED`。昇格は自動で行わず `identity_version` を伴う記録された migration とする。降格判定は「再利用可能な tier の集合」で行い、単一値比較にしない | 2026-09-16 / 監査 Phase 1.1a #6 | [security-identity.md](specs/security-identity.md) §1 |
| D-45 | レジストリ識別子が無い発行体 | 名称一致で統合しない。fallback キーは security の座標（`ISSUER-OF:<security identity key>`）。名称は `ref.issuer_names` の `ALIAS`。**false merge より false split を優先**し、stable identifier が得られた時点で identity migration として統合する | 2026-09-16 / 監査 Phase 1.1a #5 | 同上 §1・§2 |
| D-46 | 発行体名の出所 | `ref.issuers.legal_name` はレジストリ名（SEC registrant name / EDINET 提出者名）。商品名を発行体名にしない。履歴は `ref.issuer_names`（LEGAL / FORMER / ALIAS）に SCD2 で保持 | 2026-09-16 / 監査 Phase 1.1a #4 | 同上 §2 |
| D-47 | runtime worker の権限境界 | 設定・参照データ（allowlist / sources / exchanges / definitions / decision_reasons / identity_migration_map）は runtime SELECT のみ。変更は migration / DB 管理者。default privileges も SELECT のみに変更し、新規テーブルは明示 grant が無い限り書き込み不可。`DELETE` はどのスキーマにも与えない | 2026-09-16 / 監査 Phase 1.1a #1・#2 | [phase-1-universe-sync.md](runbooks/phase-1-universe-sync.md) |
| D-48 | 過去 Ticker の正本 | `ref.listing_symbols`（SCD2）。`ref.listings.local_code` は current materialized state であり as-of の正本にしない。`ref.listings_as_of()` は同一カットオフで symbol を返す | 2026-09-16 / 監査 Phase 1.1a #3 | [security-identity.md](specs/security-identity.md) §4 |
| D-49 | Rebuild 手順の再現性 | 旧→新 ID の充填は migration ではなく post-reload の手続き（`ref.finalize_identity_rebuild`）。master が空なら例外で拒否する。手順は `scripts/rebuild_security_master.sh` と runbook に残す | 2026-09-16 / 監査 Phase 1.1a #9 | 同上 §5 |
| D-50 | Coverage の診断内訳 | `provider_error_count` / `data_quality_warning_count` / `identity_collision_record_count` / `identity_collision_key_count` に分離。run_errors の全行がいずれか1つに入る | 2026-09-16 / 監査 Phase 1.1a #7 | [phase-1-universe-sync.md](runbooks/phase-1-universe-sync.md) |
| D-51 | `ref.listing_status_history` | Phase 1.1a で廃止（空・後継は `ref.listing_states`）。行が存在する環境では DROP せず DEPRECATED を明示する | 2026-09-16 / 監査 Phase 1.1a #8 | `20260916160500` |
| D-52 | Phase 2 の Raw Market Data キー | `security_id` を唯一の復元キーにしない。`provider_id` / native symbol / exchange / `observed_at` / source record id（provider security id）/ `identity_version` を必ず保存する | 2026-09-16 / 監査 Phase 1.1a #6 | [security-identity.md](specs/security-identity.md) §6 |
| D-53 | 権威ある Universe run | **publication が決める**。`pipeline.run_publications` に載った run だけが authoritative で、`pipeline.validate_run` を通らないものは publish できない。読み出しは `universe.authoritative_run_at(market, version, knowledge_cutoff)` / `universe.eligibility_as_of(...)` / `universe.current_eligibility`。`published_at <= cutoff` で絞るため、後から再構築した run は過去の Replay に逆流しない | 2026-09-16 / 監査 Phase 1.1b #4 | [security-identity.md](specs/security-identity.md) §5 |
| D-54 | DB からの HTTP 実行権限 | loader を **SECURITY DEFINER** にし、runtime role から `extensions` スキーマの USAGE と `extensions.http*` の EXECUTE を剥奪する。HTTP を出す権限は「allowlist を検査する関数」に属し、ロールには属さない | 2026-09-16 / 監査 Phase 1.1b #1 | `20260916170000` |
| D-55 | 新規 function の既定権限 | project schema では PUBLIC EXECUTE を残さない。default privileges（正の grant を作ってから revoke）+ event trigger + テスト不変条件の3段で担保する。素の `alter default privileges ... revoke ... from public` だけでは entry が作られず効かないことを実測 | 2026-09-16 / 監査 Phase 1.1b #2 | `20260916170400` |
| D-56 | JP の普通株判定 | 市場区分は「どこで売買されるか」であって種別ではない。優先株式 → `PREFERRED`、種類株式（社債型含む）→ `NOT_COMMON_STOCK`、優先出資証券 → UNRESOLVED、判定不能の特殊コード → `TYPE_UNKNOWN`。**普通株と推測しない**。universe-1.0.0 の意味変更ではなく実装の bug fix | 2026-09-16 / 監査 Phase 1.1b #3 | [universe-definition-v1.0.0.md](specs/universe-definition-v1.0.0.md) |
| D-57 | Production run の provenance | `git_sha` / `config_hash` / `job_version` / `universe_version` / `identity_version` / provider_bindings を必須化（CLI が拒否し、DB の CHECK が拒否する）。`JOB_VERSION` は出力が変わるたびに上げる | 2026-09-16 / 監査 Phase 1.1b #5 | `20260916170300` |
| D-58 | idempotency の意味 | key = job + run_mode + market + as_of + source_data_version + universe_version + identity_version + job_version + config_hash のハッシュ。**run_id を含めない**。同一 logical invocation の retry は同じ key、source snapshot や設定が変われば別 key | 2026-09-16 / 監査 Phase 1.1b #6 | [test_run_provenance.py](../workers/tests/test_run_provenance.py) |
| D-59 | 知識時刻と有効時刻 | as-of は `available_at <= knowledge_cutoff` と `effective_from <= effective_at < effective_to` の両方で絞る。2引数版が正式形、1引数版は両方同じ時刻 | 2026-09-16 / 監査 Phase 1.1b #7 | [security-identity.md](specs/security-identity.md) §4 |
| D-60 | SIC membership の provenance | SIC コード + ソート済み CIK 集合の SHA-256 を content hash とする（ページ本文は取得ごとに変わるため）。membership が変われば `source_data_version` も変わる | 2026-09-16 / 監査 Phase 1.1b #8 | `workers/src/surge/providers/sec_edgar.py` |
| D-61 | Object Storage の認証情報 | worker secret store の bucket-scoped / service credential のみ。**匿名ポリシーを作らない**。既定の投入経路は DB への直接接続（`scripts/load_snapshot_direct.py`）で、storage 経由は「job が DB へ到達できない場合」に限る | 2026-09-16 / 監査 Phase 1.1b #9 | [phase-1-universe-sync.md](runbooks/phase-1-universe-sync.md) |
| D-62 | Published run の不変性 | publish した run の artifact は INSERT/UPDATE/DELETE 禁止（trigger、owner を含む全ロール）。訂正は新 run を publish して supersede。`ref.*` は materialized 現在状態なので対象外 | 2026-09-16 / 監査 Phase 1.1c #1 | `20260916180000` |
| D-63 | publish の直列化 | run 単位の advisory lock（書き込み shared / publish exclusive）＋ publish 後の再 validate。validate と publication の間に内容が動いたら publication ごと中止 | 2026-09-16 / 監査 Phase 1.1c #2 | 同上 |
| D-64 | `data_cutoff` の定義 | `max(source_fetches.available_at)`（最後の source）。最初の source の時刻は `params.first_source_available_at` に別途保持 | 2026-09-16 / 監査 Phase 1.1c #3 | [security-identity.md](specs/security-identity.md) §4 |
| D-65 | snapshot 行の `available_at` | run が依存する全 source の `available_at` 最大値。identity 依存行が identity 解決前に見えないよう安全側（遅い側）に倒す | 2026-09-16 / 監査 Phase 1.1c #4・#7 | 同上 |
| D-66 | identifier / issuer name の provenance | `source_id` / `observed_at` / `source_record_id` は**値を供給した source**（CIK = SEC ticker file、EDINET = code list）。`available_at` は dependency max。既存行は provenance のみ修復し、値と有効期間は変えない。`available_at` は後ろにしか動かさない | 2026-09-16 / 監査 Phase 1.1c #5・#6 | `20260916180100` / `20260916180300` |
| D-67 | publication の検証範囲 | 市場一致・source_data_version 単一かつ run と一致・identity_version / universe_version 一致・全 source に `available_at`・必須 source に 64hex digest・`data_cutoff >= max(available_at)`・critical source が truncated でないこと | 2026-09-16 / 監査 Phase 1.1c #8 | `20260916180200` |
| D-68 | critical source の不完全取得 | SEC SIC（SPAC / REIT）の truncated は `CRITICAL_SOURCE_INCOMPLETE` とし **publication を拒否**する。一般の data quality 警告は拒否しない | 2026-09-16 / 監査 Phase 1.1c #8 | 同上 |
| D-69 | content_sha256 の意味 | 64桁 hex の SHA-256 か NULL。複数ファイルを1つの source として読む場合は各 digest を連結して再度 SHA-256 する（成分は run notes に保持） | 2026-09-16 / 監査 Phase 1.1c #9 | `workers/src/surge/providers/nasdaq_trader.py` |
| D-71 | 必須なのは provider ではなく dataset | 1つの provider が独立に必須な複数 dataset を供給する場合（SEC SIC 6770 / 6798）、`dataset_key` で dataset ごとに必須判定する。dataset_key を持たない既存 run は `pipeline.fetch_dataset_key` が endpoint から導出する（published run を書き換えない） | 2026-09-16 / 監査 Phase 2.0 (A) | `20260916190000` / `20260916190100` |
| D-72 | provider_bindings の粒度 | `{dataset: {provider, endpoints[]}}`。source_id で束ねると同一 provider の複数 endpoint が1つしか残らない | 2026-09-16 / 監査 Phase 2.0 (B) | `workers/src/surge/jobs/universe_sync.py` |
| D-73 | validation_version の由来 | `pipeline.validation_ruleset_version()`（現行 `publication-1.1c.0`）を publish 時の既定値にする。ruleset を変えたら上げる。**過去の publication は当時の版のまま**（`publication-1.0.0` の 2 run は現行 ruleset では validate しない。これは記録であって欠陥ではない） | 2026-09-16 / 監査 Phase 2.0 (C) | `20260916190000` |
| D-74 | ingestion_run_id と last_provenance_run_id | 前者は「その version を最初に作った run」、後者は「provenance を最後に確認・訂正した run」。再確認で前者を動かさない | 2026-09-16 / 監査 Phase 2.0 (D) | `20260916190000` |
| D-75 | metadata correction の意味 | published run の artifact は不変。`ref` master の provenance 訂正は**将来の past-cutoff query の結果を変え得る**（Phase 1.1c の「retro-hide しない」は不正確だった）。訂正そのものを `ref.provenance_corrections` に追記のみで記録し、Historical Replay はどちらを読むかを明示する | 2026-09-16 / 監査 Phase 2.0 (E) | `20260916190200` / [security-identity.md](specs/security-identity.md) §4 |
| D-79 | License Mode を第一級データにする | `market.provider_license_policies` に provider x dataset x policy_version で記録し、append-only。`assert_license_allows` は **ALLOWED 以外（PROHIBITED / NOT_SPECIFIED / UNKNOWN / policy 無し）をすべて拒否**する。規約が触れていない事項を true/false に断定せず `NOT_SPECIFIED` とする（EODHD の削除義務がこれ） | 2026-09-17 / 監査 Phase 2.1 (1) | `20260917100000` |
| D-80 | Licensed Data Purge | 全 raw object を `market.raw_objects` に登録（provider / dataset / entitlement_plan / required_min_plan / license_policy_version / sha256 / 期間）。purge は **enumerate → delete → 1件ずつ記録 → 派生行削除 → close** の4段。**dry run は完了扱いにならない**。purge は `surge_purge` ロール専用で、`surge_worker_prod` には DELETE も purge 関数の EXECUTE も無い。**J-Quants の raw object に無期限 Bucket Lock を設定しない** | 2026-09-17 / 監査 Phase 2.1 (2) | `20260917100100` / `workers/src/surge/purge.py` |
| D-81 | R2 の write-once | key は content-addressed（`raw/<provider>/<dataset>/<sha256>.<ext>`）。書き込みは常に `If-None-Match: *` を付け、**ストア自身に上書きを拒否させる**（規約ではなく機構で担保）。R2 は衝突時に **412** を返す（AWS の 409 ではない）。同一バイト列の再書き込みは `created=False` の冪等な no-op、異なるバイト列は `ImmutableObjectConflict` | 2026-09-17 / 監査 Phase 2.1 (3) | `workers/src/surge/storage/r2.py` |
| D-82 | 価格の basis を列ごとに宣言する | `open_basis` / `high_basis` / `low_basis` / `close_basis` / `volume_basis` を `RAW` / `SPLIT_ADJUSTED` / `SPLIT_AND_DIVIDEND_ADJUSTED` / `PROVIDER_UNSPECIFIED` で持つ。**EODHD の volume は `SPLIT_ADJUSTED`** であり raw と呼ばない。Provider の調整済み系列は `market.daily_bars_adjusted` に分離し、raw と同じ行に置かない | 2026-09-17 / 監査 Phase 2.1 (4)(5) | `20260917100200` |
| D-83 | 再構築した raw volume | `market.derived_volumes` に `reconstruction_method` / `split_source_dataset` / `coverage_quality` 付きで**別保存**し、vendor-native の volume を上書きしない。2018年より前に上場廃止した銘柄は splits が無く復元できないため `corporate_action_completeness = PARTIAL_KNOWN_GAP` を `market.security_coverage` に記録する | 2026-09-17 / 監査 Phase 2.1 (5) | 同上 |
| D-84 | silent overwrite の検出 | J-Quants は版番号も ETag も差分も持たず訂正を上書きで行う。rolling refetch で digest を比較し、変化を `market.source_revisions`（object 単位）と `market.daily_bar_revisions` / `market.fx_rate_revisions`（行単位、trigger）に記録する | 2026-09-17 / 監査 Phase 2.1 (4)(7) | 同上 |
| D-85 | EODHD の API quota 会計 | bulk は **HTTP リクエスト1本 = API コール100本**。`QuotaLedger` は provider が数えるもの（コール）を数え、transport が数えるもの（リクエスト）ではない | 2026-09-17 / 監査 Phase 2.1 (6) | `workers/src/surge/providers/eodhd.py` |
| D-86 | ECB FX の保存形 | 両レッグ（`eur_usd` / `eur_jpy`）を raw 保存し、`derived_usd_jpy` と `derivation_method` を併記。`rate_kind = REFERENCE_RATE`（取引レートではない）。`source_published_at` は `Last-Modified`、`available_at` は**自分の受信時刻**。「翌営業日以降は改訂されない」を仮定せず rolling refetch で hash 比較する。**16:00 CET = xx ET のような固定変換をコードに埋めない** | 2026-09-17 / 監査 Phase 2.1 (7) | `20260917100200` / `workers/src/surge/providers/ecb_fx.py` |
| D-87 | FX Eligibility の読み方 | `market.usdjpy_as_of(cutoff)` は `available_at <= cutoff` の最新1件を返し、`source_date` と `fx_age_seconds` を同時に返す。**同日レートが無いとき直前レートを同日扱いにしない**（rate_date と price_date を別に保存する） | 2026-09-17 / 監査 Phase 2.1 (8) | 同上 |
| D-88 | OpenFIGI は Research Enrichment | 全 match を `market.figi_mappings` に保存し、`mapping_status` を EXACT / AMBIGUOUS / UNMAPPED / ERROR で持つ。**複数 match を自動で1件に決めない**（`FRCB` は2発行体を返す）。**既存 security_id を変更しない**。昇格は coverage report と identity migration 監査の後に限る。CUSIP は Security Master へ新規取得も保存もしない | 2026-09-17 / 監査 Phase 2.1 (9) | 同上 / `workers/src/surge/providers/openfigi.py` |
| D-89 | availability_basis | `OBSERVED_NOW` / `PROVIDER_PUBLISHED_TIMESTAMP` / `DOCUMENTED_SCHEDULE` / `HISTORICAL_REPLAY_ASSUMPTION`。取り込みは**常に `OBSERVED_NOW`**（今日取った10年前の bar を、10年前に観測したことにしない）。`source_published_at` は provider の主張として別に保存し、別の availability model を使う Replay は basis を明示して宣言する | 2026-09-17 / 監査 Phase 2.1 (10) | `20260917100100` |
| D-90 | project schema の一覧を1箇所にする | PUBLIC EXECUTE 剥奪の対象スキーマを3箇所に書いていたため、`market` 追加時に全関数が public 実行可能なまま残った（CI が検出）。`pipeline.project_schemas()` を正本とし、event trigger・sweep・テストがこれを読む | 2026-09-17 / Phase 2.1 実装中に発見 | `20260917100300` |
| D-91 | J-Quants `ProdCat` | 2026-05-26 に JPX が追加した商品区分（011 内国株券 / 012 優先出資証券 / 013 REIT / 014 ETF / 021-024 外国）。Phase 1 は種別を銘柄名から推定していたが、これはレジストリが種別を宣言している。**ただし普通株と優先株式・種類株式は両方 011 なので銘柄名判定を置き換えない**。突き合わせて差分を監査する | 2026-09-17 / Phase 2.1 仕様確定時に発見 | [phase-2-1-provider-api-specs.md](research/phase-2-1-provider-api-specs.md) §1-3 |
| D-94 | Zero-Cost Core を構造で担保する | `market.providers.cost_class` と `market.provider_role_bindings` で表現。paid provider は削除せず `OPTIONAL_PAID` + `enabled=false` + `priority=9` で保持し、`market.recurring_cost` が 0 を返すことをテストで検査する。役割が埋まっていないことは `market.unfilled_roles` で可視化し、沈黙させない | 2026-09-17 / 方針変更 Zero-Cost Core | `20260917110300` |
| D-95 | Provider 評価結果をデータとして残す | 却下した候補と**その決め手になった条項**を `market.provider_evaluations` に記録する。「高かった」と「規約が保存を禁じている」では次の一手が違うため、理由まで残す。検証で結論が逆転した2件は `reversed_by_verification` で明示 | 2026-09-17 / 同上 | `20260917120000` / [zero-cost-provider-evaluation.md](research/zero-cost-provider-evaluation.md) |
| D-96 | 比較可能系列は自前で作る | Vendor の adjusted 系列は分割のたびに遡って再計算されるため as-of 時点の事実ではない。raw 価格と**as-of 時点で既知の**コーポレートアクションだけから `SPLIT_ADJUSTED_TO_AS_OF` を構築する。読めない分割が1件でもあれば、その銘柄の feature は**一切生成しない** | 2026-09-17 / Phase 2 実装 | `workers/src/surge/market/series.py` |
| D-97 | Vendor が調整済みの volume の扱い | provider が自分の取得時点へ調整した volume は、as-of 以降に分割があると as-of 基準へ戻せない（未来情報が要る）。その場合 volume を**落として落としたと記録する**。推測で埋めない | 2026-09-17 / 同上 | 同上 |
| D-98 | Stage 1 は数値を正本にする | `screening.features_daily` に約60の数値 feature を保存し、Route は**発火した測定値を `route_evidence` に残す**。pattern label だけにしない。Route は OR 型で `discovery_routes[]` に全て残す | 2026-09-17 / Phase 3 実装 | `20260917110200` |
| D-99 | Route F の turnover 閾値は市場別 | turnover は当該証券の通貨建ての金額なので、単一閾値では米国銘柄が日本銘柄の100分の1に見える。`max_turnover_avg_20d_jpy` と `max_turnover_avg_20d_usd` を分ける。turnover を供給しない provider では Route F は**発火しない**（coverage gap として記録） | 2026-09-17 / 同上 | 同上 |
| D-100 | 空の1日は失敗として扱う | 休場日と障害は同じ「0行」に見える。取得ジョブは空を `EMPTY_RESULT` の失敗として返し、取引カレンダーを知る呼び出し側が判断する。検証済みカレンダーが無いうちに休場日表を推測で作らない | 2026-09-17 / Phase 2 実装 | `workers/src/surge/market/ingest.py` |
| D-101 | 3000円フィルタの staleness 境界 | `price-filter-1.0.0`: 閾値3000円、価格は as-of から5暦日以内、FX は 345,600秒（4日）以内。超過は `STALE_PRICE` / `STALE_FX` として**独立の結果**にする。境界は「止まったフィードを捕まえる」ためのもので、静かな銘柄を排除するためではない。変更は新 rule_version | 2026-09-17 / Phase 2 実装 | `20260917110100` |
| D-106 | Phase ごとの停止監査を廃止 | 連続実装へ移行。停止するのは6つ（契約・支払い / credential・account 操作 / 法的・利用規約上の本人確認 / 不可逆な削除 / Canonical 仕様との正面衝突 / プロジェクト目的の変更）だけ。可逆・安全側・versioned に決められる事項は自分で決めて記録する | 2026-09-17 / ChatGPT 監査（Phase 2 Core・Phase 3 Stage 1 承認） | [CLAUDE.md §3](../CLAUDE.md) / [development-phases.md](development-phases.md) |
| D-107 | `IMPLEMENTED_NOT_LIVE_VERIFIED` を状態として持つ | Provider 未確定・credential 未取得の領域は実装を止めず、interface と schema と pipeline を完成させ、入力は合成 fixture で満たす。**「実データで検証していない」ことを報告と DB の両方に明示する。** `ui.not_live_verified` が画面に出す | 2026-09-17 / 同上 | `news.sources.live_verified_at` / `analysis.llm_providers.live_verified_at` / `chart.concept_evidence_grade` |
| D-108 | 材料の `available_to_model_at` は取得時刻から導出する | 4つの時刻のうち**判断を制御するのはこれだけ**。`source_published_at` から導出しない。backfill した記事は backfill した時刻に知り得たのであって、公表時刻に知り得たのではない。replay が反実仮想を使いたい場合は `replay_assumed_available_at` に**理由を書いて**分離し、production の読み出し関数はその列を返さない | 2026-09-17 / Phase 4 実装 | `20260917130000` / `workers/src/surge/news/models.py` |
| D-109 | 全文保存は明示許諾がある source のみ | source policy が `full_text_storage_allowed = ALLOWED` でない限り `METADATA_ONLY` で保存し、**body を取得しにも行かない**。沈黙は `NOT_SPECIFIED` であって許諾ではない。DB 側でも insert と同一トランザクションで `news.assert_storage_allows` を呼び、Python 側の判断が古くても書けないようにする | 2026-09-17 / Phase 4 実装 | `20260917130000` / `workers/src/surge/news/collector.py` |
| D-110 | news は自前の object manifest を持つ | `market.raw_objects` は market の licence policy と provider plan に3本の FK で縛られており、日銀のプレスリリースを入れるには中央銀行用の market data 購読プランを捏造する必要があった。`news.raw_documents` を分ける。共有するのは purge request だけ（ライセンス義務は1つなので、半分だけ履行できる形にしない）| 2026-09-17 / CI が検出 | `20260917150000` |
| D-111 | Event merge は false split を優先する | 同一性と同じ非対称性: 分かれたものは後で統合できるが、統合したものは戻せない。統合するのは (a) 同一 entity かつ**分単位で一致する公表時刻**、(b) 明示的な相互参照、の2つだけ。それ以外は分けたまま**「統合候補」として一覧に出す**（見えない split は悪い merge と同じくらい悪い）| 2026-09-17 / Phase 5 実装 | `workers/src/surge/material/merge.py` |
| D-112 | Discovery / Verification / Corroboration は link の属性 | publisher の属性ではない。同一 publisher の続報は CORROBORATION であって確認ではない。`independent_source_count` は DISCOVERY と VERIFICATION の**distinct publisher** のみを数える。IR > News の固定順位は作らない（`news.sources.fetch_priority` は取得順であって材料の強さではない、とコメントで明記）| 2026-09-17 / Phase 5 実装 | `20260917140000` |
| D-113 | Noise filter は構造シグナルだけで判定する | キーワード除外は禁止。判定は resolved entity 数 / 因果経路の有無 / 規制開示か / 定量化された magnitude から行い、**根拠とした signals を必ず保存**する。`UNCERTAIN` は除外ではない（mechanism を抽出できなかった macro は保持してレビューへ）。`NOT_RELEVANT` は「紐付く証券が1つも無い」場合のみ、それでも event 行は残す | 2026-09-17 / Phase 5 実装 | `workers/src/surge/material/relevance.py` |
| D-114 | Material Route は M1〜M6、`WEAK_ASSOCIATION` はどの route も受け取らない | engine・route 定義表・CHECK 制約の3箇所で担保。macro route (M4/M5) は causal_path が無いと発火しない。**測定できなかった feature は閾値を超えない**（None を通過扱いにしない）| 2026-09-17 / Phase 5 実装 | `20260917140000` / `workers/src/surge/material/routes.py` |
| D-115 | Chart concept は mechanism・negative context・counterexample が無いと保存できない | さらに **failure example が1件も無い concept は enable できない**（trigger）。「pattern 名の辞書にしない」を制約にした。seed した6 concept は synthetic example で enable しているため `chart.concept_evidence_grade` が全件 `SYNTHETIC_ONLY` を返す。合成例は mechanism を示すが証拠ではない | 2026-09-17 / Phase 6 実装 | `20260917160000` |
| D-116 | Stage 2 は数値が正本、concept 名は導出 | label から測定値は復元できないが、測定値から label は復元できる。測定できなかったものは `measurement_gaps` に名前を書く。volume が as-of の株数へ戻せない系列では **volume 系の測定を一切出さない**（単位の違う2つの比を ratio と呼ばない）| 2026-09-17 / Phase 6 実装 | `20260917160000` / `workers/src/surge/chart/stage2.py` |
| D-117 | 価格障害は upside にならない構造にする | `ObstacleReport.upside_from_obstacles()` は常に例外を投げる。「どこまで行けるか」を探すコードが現価格より上の水準リストに手を伸ばすのは自然なので、間違いの着地点を用意した。overhang は失効するので、減衰定数ではなく**水準ごとの `weakening_evidence`** で表現する | 2026-09-17 / Phase 6 実装 | `workers/src/surge/chart/obstacles.py` |
| D-118 | Stage 3 に ENTRY state を作らない | enum に member が無いので表現不能。EOD 分析は live price を持たないので entry を出せない。出力検証器は「buy now」等の指示的な文言と「旧高値まで戻る」型の upside 表現も reject する（抵抗としての言及は歓迎するので、言及ではなく**復帰の枠組み**にマッチさせる）| 2026-09-17 / Phase 7 実装 | `20260917170000` / `workers/src/surge/analysis/validate.py` |
| D-119 | 20% Threshold と Reachable Zone を列ごと分ける | 前者は参照価格への算術、後者は到達可能性の判断。`reachable_zone_basis_kinds` は `PRIOR_HIGH` を含まない閉集合で、zone を出すなら1つ以上が必須（CHECK）。両者が同じ値になったら検証器が reject する（片方がもう片方から導出されている）| 2026-09-17 / Phase 7 実装 | `20260917170000` |
| D-120 | 有料 LLM を Production 必須にしない | `LLMProvider` interface と deterministic mock で pipeline を完成させ、実モデル接続は `analysis.llm_providers` の1行にする。mock は**自分が mock であることを rationale に書く**（結果表に無記名の mock 判定が混ざるのは罠）| 2026-09-17 / Phase 7 実装 | `20260917170000` / `workers/src/surge/analysis/llm.py` |
| D-121 | Web は `surge_web` で接続する | `surge_readonly` はベーステーブル79個を読めるので、「画面は契約しか見ない」が規約でなく事実になるよう専用ロールを作った。`ui` スキーマの USAGE と view の SELECT だけを持ち、契約の外へ手を伸ばすと権限エラーになる。読み出し関数2つは同じ理由で SECURITY DEFINER | 2026-09-17 / UI 実装 | `20260917190000` |
| D-122 | ~~**TDnet は採用しない**~~ → **2026-09-17 撤回（D-125 が置き換え）** | **当初の判定**: (a) `release.tdnet.info/robots.txt` が全ホスト `Disallow: /`、(b) 開示ページが「無断で転用、複製又は販売等を行うことは固く禁じます」、(c) 有料 API は 月額70,000円 + 100,000円〜。**この3点は今も正しい**。誤っていたのは「したがって JP 適時開示は取得不能」という結論のほうで、**TDnet 本文**の取得不能から**適時開示の存在・時刻・発行体**の取得不能を導いてしまった。index と body は別物で、前者を提供する第三者 API が存在する（D-125）。**TDnet 本文を保存しない**という部分は撤回しない | 2026-09-17 / ユーザー指示により撤回 | [yanoshin-tdnet-api.md](research/yanoshin-tdnet-api.md) |
| D-123 | 却下した news source もレジストリに残す | `enabled=false` + 全 permission `PROHIBITED` の policy 付きで登録する。後からコレクタを書こうとすると `assert_may_collect()` が条項を添えて落ちる。market 側と同じ「一度却下したものは理由つきで却下のままにする」原則 | 2026-09-17 / 同上 | `20260917210000` |
| D-124 | robots と licence は別の列で持つ | JPX が実例: robots.txt は巡回を許可し、規約は保持を許可しない。「取りに行ってよいか」と「持っていてよいか」は別の問いなので `robots_allows_path` と `full_text_storage_allowed` を分ける | 2026-09-17 / 同上 | `20260917130000` |
| D-125 | **Yanoshin TDnet WebAPI を JP 適時開示の Discovery Source として採用** | 認証不要・0円。`news.sources.yanoshin_tdnet`。運営自身が「当サービスはインデックス情報を提供するものであり…書類データは提供元のサイトを必ずご確認ください」と書いており、index と body の線引きは**運営の言葉どおり**。robots.txt は `*.json` 等を **Googlebot にのみ**禁止し、他の全 UA には `Allow:/` | 2026-09-17 / ユーザー指示 | `20260917220000` / [yanoshin-tdnet-api.md](research/yanoshin-tdnet-api.md) |
| D-126 | **Yanoshin の利用可能性と TDnet 本文の保存許諾を同一視しない** | policy の `full_text_storage_allowed = PROHIBITED`。`news.assert_storage_allows` が全文書き込みを拒否し、adapter の `fetch_body()` は無条件に `None` を返す。本文の Verification は issuer 公式 IR / EDINET など**保存許諾が明示された source** から取り、同一 event に source link を足す | 2026-09-17 / 同上 | `20260917220000` / `workers/src/surge/news/sources/yanoshin_tdnet.py` |
| D-127 | **`since_id` / `by_id` は使わない**（documented だが実装されていない） | `llms.txt` は `since_id` を「ID より新しい item を返す」と明記するが、実測では **Unix timestamp として解釈**される（`since_id=1281122` → 「期間開始日1970/01/16」、`by_id=1281122` → 「期間終了日1970/01/16」で0件）。documented どおりに実装するとカーソルが永久に進まないか、常に0件で「静かな日」に見える。adapter は両パラメータを**例外で拒否**する。カーソルは `last_seen_id` の**クライアント側** high-water mark、日付レンジは `YYYYmmdd-YYYYmmdd` と `start_datetime`/`end_datetime`（こちらは正しく動く） | 2026-09-17 / 実測 | 同上 |
| D-128 | id は insert 順であって publication 順ではない | 実測: id 1281124 が 12:20、id 1281125 が 12:15。**新着判定の watermark としては正しく、時刻のソートキーとしては誤り**。`recent` が満杯で返ったら truncate された可能性があるので `window_is_safe()` が false を返し、呼び出し側は日付レンジで読み直す（初回の live pass で実際に発火） | 2026-09-17 / 実測 | 同上 |
| D-129 | company_code の末尾0を無条件に削除しない | 5文字目が `0` のときだけ落とす（`72030`→`7203`、`130A0`→`130A`）。`0` 以外なら**5文字すべて保持**して Security Master へ照合する（`587A4`、`13264`、`15574` はいずれも ETF/ETN）。実データ300件では 280 strip / 20 keep で、keep された20件は全て fund。`raw_company_code` を必ず保存し、DB の CHECK 制約が branch と結果の整合を強制する | 2026-09-17 / 実測 | 同上 |
| D-130 | Discovery 時刻は後から来た公式 IR で上書きしない | `material.events.first_known_at` は source 群の `min(available_to_model_at)`。後から届く VERIFICATION source は時刻が遅いので min は動かない。**コードの規律ではなく trigger の性質**として成立する | 2026-09-17 / 同上 | `20260917140000` / `workers/tests/test_db_tdnet_discovery.py` |
| D-131 | タイトル分類は type を付けるだけで、除外はしない | 本文が取れないので headline から type を導く。未知タイトルは `OTHER` になり、**それでも material candidate を生成する**（relevance は Phase 5 が構造シグナルで判定する）。confidence は常に `PROVISIONAL`、マッチしたパターンを evidence に残す。訂正・開示事項の経過は type ではなく**フラグ**（訂正された決算短信は依然として決算短信） | 2026-09-17 / 同上 | `workers/src/surge/material/tdnet_classify.py` |
| D-132 | mapping confidence は relation へそのまま運ぶ（再宣言しない） | `from_tdnet.to_relation()` が全件 `REGISTRY_ANCHORED` を返していた。exact 一致は `REGISTRY_ANCHORED`、4文字ベースでしか当たらなかったものは `PROVISIONAL` で、**PROVISIONAL のまま**運ぶ。境界で言い直すと弱い主張が誰も見ていない場所で強い主張に昇格する。cloud smoke の実データで 8 exact / 4 base、`news.tdnet_items.mapping_confidence` と `material.entity_relations.confidence` が12件すべて一致 | 2026-09-17 / ChatGPT 統合監査 | `workers/src/surge/material/from_tdnet.py` |
| D-133 | TDnet code の解決は bitemporal（現在行への直接クエリを使わない） | `ref.listings_as_of(effective_at, known_at)` を通す。`effective_at` = 開示の `pubdate`（当時どの上場が有効だったか）、`known_at` = その item を得た時刻（master が何を知っていたか）。**今の master で過去の開示を解決するのは可**、**当時それを知っていたことにするのは不可**。cache key は `(code, effective_at 日, known_at 日)` の3要素で、code だけで引くと最初の答えが以後すべての質問に居座る | 2026-09-17 / 同上 | `workers/src/surge/jobs/tdnet_discovery.py` / `workers/tests/test_db_tdnet_resolver.py` |
| D-134 | Authoritative Universe Gate は Material 側にも掛ける | Technical / Material の**ルート独立は維持**する。universe はルートではなく「この基盤が何を予測対象にするか」の全体規則なので、片側だけに掛けると ETF が開示経由で Stage 3 に入る。`INCLUDED` → 正式候補可、`UNRESOLVED` → event・relation・feature は保存し research からは見えるが正式候補にしない、`EXCLUDED` → event は保存、Stage 2 / Stage 3 に入れない。**universe を読んでいない場合も `UNRESOLVED`**（読まなかったことは通行許可ではない）。cloud smoke の実データで ETF 4件が EXCLUDED、候補生成 0件 | 2026-09-17 / 同上 | `workers/src/surge/material/universe_gate.py` |
| D-135 | `after_close` を 3値にする（`PRE_CLOSE` / `POST_CLOSE` / `UNKNOWN`） | 検証済み取引カレンダーが無い間は `UNKNOWN`。`UNKNOWN` からは `TECHNICAL_SETUP_EOD` も `POST_CLOSE_CATALYST_SETUP` も**主張しない**（`WATCH_OTHER` に落とす）。引けが既に織り込んだかどうかが両者を分けるので、分からないときに都合のよい方を選ばない。field 欠落は `None` ではなく `UNKNOWN` へ正規化する | 2026-09-17 / 同上 | `workers/src/surge/material/models.py` / `workers/src/surge/analysis/llm.py` |
| D-136 | classifier の confidence と mapping の confidence を同じ名前にしない | 同じ evidence JSON に並ぶので、前者を `classification_confidence` に改名した。`"confidence": "PROVISIONAL"` が `mapping_confidence` の隣にあると**同じ問いへの2つ目の意見**に読めるが、実際には「開示の種類がどれだけ確かか」と「どの銘柄の話かがどれだけ確かか」で別の問いである | 2026-09-17 / cloud smoke の生成 SQL を読んで発見 | `workers/src/surge/material/tdnet_classify.py` |
| D-137 | Cloud smoke は production の INSERT 文をそのまま描画して流す | cloud へは SQL 実行経路しか無いため、`news.db` / `material.db` の**文そのもの**を読み込み、`%(name)s` を literal に置換して1トランザクションにする。列リストと cast を smoke 側で書き直すと、migration との差異が smoke の成功として通ってしまう。DB が採番する `document_id` / `event_id` のみ自然キーの副問い合わせに置換する。**2026-09-17 実行結果**: documents 12（body_text 0 / 全件 `METADATA_ONLY`）、`news.raw_documents` 0、tdnet_items 12（全件 mapped）、events 12、event_sources 12（全件 `DISCOVERY`、`VERIFICATION` 0）、entity_relations 12、features 12、coverage 1、`material.candidates` 0、`first_known_at` = discovery 時刻 12/12。**全文取得 0 / TDnet PDF・XBRL body 0** | 2026-09-17 / ユーザー指示の1回限りの smoke | `workers/src/surge/jobs/tdnet_smoke.py` |
| D-138 | 引けの分析は ENTRY を出せない、を DB 制約で持つ | `prod.decision_state` には `ENTRY` がある（`analysis.stage3_state` には無い）。場中判断は実際にその状態へ到達するため。代わりに **行ごとの CHECK** で `analysis_kind in ('ENTRY_DECISION','REANALYSIS')` を要求する。`prod.setups` と `prod.entry_attempts` と `prod.predictions` の3か所に同じ制約を置いた | 2026-09-17 / Phase 8 実装 | `20260917230000` / `workers/src/surge/entry/decision.py` |
| D-139 | `TRIGGER_HIT → ENTERED` という辺を作らない | Watch 条件への到達は**観測**、ENTRY は**判断**。`TRIGGER_HIT → IN_REANALYSIS → ENTERED` だけが通る。再分析は「やめる」と言ってよく、それが再分析を走らせる理由そのもの。Python の state machine と DB trigger の**両方**に持つ（前者は誤った job に明確なエラーを出し、後者は将来 Python を経由しない経路ができても効く） | 2026-09-17 / 同上 | 同上 / `workers/src/surge/entry/watch.py` |
| D-140 | Prediction は3条件を全部満たさないと作らない | (a) `entry_reference_price` が存在する（欠落は D-31 の `NO_ENTRY_REFERENCE_PRICE`。`decision_price` で代用しない）、(b) provider が deterministic mock でない、(c) universe が `INCLUDED`（`UNRESOLVED` は除外ではないが包含でもない）。3つとも CHECK 制約と Python の両方。加えて `target_price = entry_reference_price × 1.20` を CHECK で強制し、`decision_price` から計算できないようにした | 2026-09-17 / 同上 | 同上 |
| D-141 | 3,000円は decision と entry の**2つの価格**で2回判定する | 別々の問い。`decision_price` は「そもそも検討してよい銘柄か」、`entry_reference_price` は「実際に買えた価格がルール内か」。前者を通り後者で外れる場合は `ENTRY_ABORTED_PRICE_LIMIT` として記録し、Prediction も Episode も作らない。再び3,000円以下になっても自動復活させず、新しい判断として扱う | 2026-09-17 / 同上 | 同上 / `workers/tests/test_entry_and_episodes.py` |
| D-142 | Horizon は「実際に立った session」から数える（カレンダーを推測しない） | S0 = `entry_price_observed_at` の session、S20 close が Primary Horizon。検証済み取引カレンダーが無い（D-135）ので、未観測の session index を訊かれたら `SessionCalendarUnknown` を上げる。推測した休場日から horizon 境界を作ると、Prediction の期限という実データに捏造した日付が入る。`horizon_sessions = 20` は CHECK 制約で固定（長くして救えないようにする） | 2026-09-17 / 同上 | 同上 / `workers/src/surge/entry/episode.py` |
| D-143 | `initial_failure_line` と `current_risk_line` を別テーブルに置く | 前者は `prod.predictions` に固定（append-only trigger で変更不可）、後者は `prod.risk_line_updates` の append-only な列。**risk line に触れても Episode はクローズしない** — それが risk line を動かしてよい理由であり、同時に成績評価に使えない理由でもある。1つの列にまとめると、動く方が動かない方を飾りにする | 2026-09-17 / 同上 | 同上 |
| D-144 | Episode は (security, thesis) ごとに1本（部分 unique index） | 同一 thesis の再 ENTRY は `REAFFIRMED_EXISTING_EPISODE` として記録し、Prediction を作らず horizon もリセットしない。Python 側だけで判定すると自分自身と競合するので、`where status = 'OPEN'` の partial unique index を正本にした。`thesis_key` は D-17a が未確定のため暫定（state + 発火した route）で、**呼び出し側から渡された値をそのまま保存**する。後で定義を変えるときに過去の Episode を黙って読み替えないため | 2026-09-17 / 同上 | 同上 / `workers/src/surge/entry/from_stage3.py` |
| D-145 | Entry attempt は「何も生まなかった判断」も1件ずつ残す | 7つの status のうち6つは Prediction を作らない。作らなかった判断を残さないと、そこから計算した勝率は分母が欠けた数字になる。`ui.entry_attempt_ledger` は成功と不成功を**同じ view**に入れる（別 view にすると誰も開かない方に不都合な行が溜まる） | 2026-09-17 / 同上 | 同上 / `apps/web/src/app/entries/page.tsx` |
| D-146 | Watch transition は宣言された from_state を信用せず、watch の現在値を行ロックで読む | **実際に穴だった**（Cloud で実証後 rollback）: `case new.from_state when null then ...` は simple CASE なので `=` 比較になり NULL に一致しない → `legal` が NULL → `if not NULL` が真でない → **新規 watch が ENTERED へ直行できた**。修正: `select ... for update` で現在値を取得 / 初回のみ明示 `IS NULL` 分岐（`NULL -> ARMED` かつ1回だけ）/ `from_state IS NOT DISTINCT FROM current_state` を必須化 / trigger を SECURITY DEFINER にして head 更新を内部化し、**worker から `prod.watches` の UPDATE 権限を剥奪** | 2026-09-17 / ChatGPT Phase 8 監査 | `20260917240000` / `workers/tests/test_db_entry_lifecycle.py` |
| D-147 | Prediction は attempt の15列と episode の3属性に一致しなければならない | 元は3列だけ照合していた。security / thesis / 通貨換算値 / provider / cutoff / run_id が食い違っても通った — **正直な attempt の隣に都合のよい prediction を書く**形がちょうど通る。`IS DISTINCT FROM` で NULL semantics を正しく扱い、不一致列を全部挙げて raise する。episode 側も security / thesis / `entry_price_observed_at`（= S0）の一致を要求 | 2026-09-17 / 同上 | 同上 |
| D-148 | `prod.setups` は append-only、`prod.episodes` は OPEN→CLOSED の1回だけ | episode の identity 列（security / thesis / opened_at / `entry_price_observed_at` / horizon / verification）は凍結。**`entry_price_observed_at` を動かすと S0 が動き horizon が動く** — outcome が見えた後にそれをやれてはいけない。CLOSED は terminal（再 open も close_reason 変更も不可） | 2026-09-17 / 同上 | 同上 |
| D-149 | +20% は tolerance 比較をやめ `target_price = round(entry × 1.20, 6)` の等号にする | numeric 同士に許容誤差は要らず、tolerance は誤った数値が座れる場所になる。Python 側は `ROUND_HALF_UP` で Postgres の `round()` に合わせ、両実装を直接突き合わせる fixture を置いた | 2026-09-17 / 同上 | 同上 / `workers/src/surge/entry/models.py` |
| D-150 | パス解決の「分からない」は2種類あり、混ぜない | `AMBIGUOUS_PATH` = **より細かいデータが市場に存在しない**ので順序は原理的に不明。`UNRESOLVED_MISSING_DATA` = **あるはずのデータが届かなかった** = 取得の不具合で、直せる。後者を前者に混ぜると bug が「仕方ない」の下に隠れる。どちらが該当するかは「その市場が公開しているか」という**明示的な入力**で決め、データの不在から推測しない | 2026-09-17 / Phase 9 実装 | `workers/src/surge/outcome/path.py` |
| D-151 | S0 は entry 時刻より後だけを読む（日足は使わない） | S0 の日足は setup を生んだ上昇を含むので、そこから high を採ると**既に過ぎた価格**を prediction の手柄にする。S0 は約定 → 分足の順で entry 時刻以降のみ。無ければ `UNRESOLVED_MISSING_DATA` | 2026-09-17 / 同上 | 同上 / `workers/src/surge/outcome/engine.py` |
| D-152 | 分割は系列を ENTRY 時点の株数へ restate し、target と failure line は動かさない | `comparable_price = raw × R`（R = entry 後の ex 済み比率の積）。**系列を線に合わせるのが正しく、線を系列に合わせるのは主張の書き換え**。記録のない分割形の不連続（既知比率の±2%以内）は `CORPORATE_ACTION_SUSPECTED` として**確定させない** — 分割を failure として記録した数値は誰も後から疑わない。配当は SplitAction に列が無く、構造的に加算不能 | 2026-09-17 / 同上 | `workers/src/surge/outcome/comparable.py` |
| D-153 | Outcome engine に FX 入力を作らない | 銘柄の取引通貨だけで計算し、通貨混在の系列は例外で落とす。**「+17%の USD 上昇が円安で +20% 成功になる」経路が構造的に存在しない**のが 1-9 の実装 | 2026-09-17 / 同上 | `workers/src/surge/outcome/engine.py` |
| D-154 | Episode の close と outcome 保存は1つの関数呼び出し | `prod.close_episode_with_outcome()`（SECURITY DEFINER）。呼び出し側の transaction でも同等だが、**「outcome の無い closed episode」を表現不能にする**方が強い。trigger が「outcome の verdict ≠ episode の close_reason」も拒否する（同じ事実を2回違う答えで書かせない） | 2026-09-17 / 同上 | `20260917250000` |
| D-155 | Objective 層は「ラベル」ではなく「測定値」として持つ | `hit_20` のような単一 binary は計算が簡単で ground truth に見え、**「値動きの予測」を教えてしまう** — 本システムが問うているのは「自分の判断が正しかったか」で、そちらには failure line がある。学習対象は admission policy を通った Interpretive label のみ。policy は**3クラス未満を CHECK 制約で拒否**（2クラスはほぼ必ずあの binary の言い換え）。`hit_10/20/30` と `failure_line_hit` は target 名として明示的に禁止 | 2026-09-17 / Phase 10 実装 | `20260917260000` / `workers/src/surge/labels/admission.py` |
| D-156 | 見逃しの3分類は情報集合を物理的に分けて判定する | A（システムが実際に持っていた）→ `ACTIONABLE_FALSE_NEGATIVE` / B（市場にはあったが collector 遅延で持っていなかった）→ `PIPELINE_MISSED_ACTIONABLE_SIGNAL` / C（事前情報なし）→ `OUT_OF_SCOPE_SHOCK`。**A の判定器に B の文書を見せた瞬間に3分類は崩壊する**（signal が自明に見え、収集障害が model の失敗として記録される）ので `assert_no_leakage()` が明示的に拒否する。`source_published_at` は発行者の自己申告なので `source_timestamp_trusted` を既定 false にし、reason にも書く。**B・C・LATE は admission policy が拒否**する（モデルに自分のせいでないことの責任を負わせない） | 2026-09-17 / 同上 | `workers/src/surge/labels/misses.py` |
| D-157 | 教師母集団は Eligible 全銘柄（Prediction を作った銘柄だけにしない） | `observation_kind` = PREDICTED / SETUP_NOT_ENTERED / ELIGIBLE_ONLY。PREDICTED だけで学習すると**本システムが既に信じていたことを学ぶ**だけになり、見逃しがモデルから見えなくなる。未解決パスの `hit_20_before_failure` は false ではなく **null**（「分からなかった」と「起きなかった」は別の事実で、証拠になるのは片方だけ） | 2026-09-17 / 同上 | `20260917260000` / `workers/src/surge/labels/objective.py` |
| D-158 | Model Lab は器だけ作り、promotion gate は今すべて拒否する | walk-forward は**時系列のみでシャッフル分割の引数が存在しない**。purge gap は horizon 相当（30日）を下限とし、短い gap は例外。加えて `assert_no_leakage()` が `(dated_on, settled_on)` を見て「学習ラベルの決着が test window の中」を捕まえる（境界だけ見る検査では見えない、休場・売買停止・S20超過の場合）。Champion 比較は**同一 fold** 必須・引き分けは勝ちにしない・無対戦の challenger は None。gate は実データ無し / ラベル不足 / 3クラス未満 / 薄いクラス / fold 不足 / calibration 無し / 人手承認無しを**全部列挙して**拒否し、`test_today_the_gate_refuses_everything` がその状態を assert する（コメントではなくテストにしたので、変わるときは誰かが必ず考える） | 2026-09-17 / Phase 11 実装 | `workers/src/surge/modellab/` |
| D-159 | S20 未到達の Outcome は確定させない（`HORIZON_EXPIRED` を仮置きしない） | `HORIZON_EXPIRED` は「S20 まで**どちらにも触れなかった**」という S20 についての主張なので、S3 で言えない。未決着かつ 21 session 未観測なら `primary = None` / `pending_reason = HORIZON_INCOMPLETE` / `is_final = False` を返す。**到達した線はその場で確定**し S20 を待たない。記録の無い分割形 gap も同様に pending（`CORPORATE_ACTION_SUSPECTED` を close_reason として書くと episode をそれで閉じてしまう）。`outcome_params()` は final でない report を `OutcomeNotFinal` で拒否 | 2026-09-17 / ChatGPT Phase 9-11 監査 | `workers/src/surge/outcome/engine.py` / `workers/tests/test_outcome_engine.py` |
| D-160 | `HORIZON_EXPIRED` は DB 制約でも 21 session を要求する | `counterfactual_sessions_observed = 21` かつ `primary_resolved_session_index = 20`、かつ `counterfactual_later_target_hit is false`（全期間観測して未到達なら「到達しなかった」は所見である）。application だけで守ると、将来の別 writer が S8 で書ける | 2026-09-17 / 同上 | `20260917280000` |
| D-161 | Episode の close は `prod.close_episode_with_outcome()` だけ | **実際に穴だった**: `surge_worker_prod` が `prod.episodes` を直接 UPDATE できたので、`OPEN -> CLOSED` だけして outcome を書かない状態を作れた。修正: episodes の UPDATE と episode_outcomes の INSERT/UPDATE を worker から**剥奪**し、close 関数が transaction-local flag を立てている間だけ両 trigger が通す。**insert が失敗すれば close も rollback**（Cloud で実証: 失敗後も episode は OPEN）。`episode_outcomes` は append-only | 2026-09-17 / 同上 | 同上 / `workers/src/surge/entry/db.py`（直接 close を削除） |
| D-162 | `later_target_hit` は3値（TRUE / FALSE / NULL） | `coalesce(p_later_target_hit, false)` を削除。TRUE = 到達を確認 / FALSE = **全期間観測して**未到達 / NULL = 不明（期間未完了、またはパス未解決）。NULL を false へ潰すと「まだ見終わっていない」が「起きなかった」になり、2つのうち都合のよい方に倒れる。UI も reached / not reached / unknown を分ける | 2026-09-17 / 同上 | `20260917280000` / `workers/src/surge/outcome/models.py` |
| D-163 | Production teacher table は 0 件から始める（0-start を固定） | Cloud 実測で `objective_labels` / `interpretive_labels` / `pipeline_miss_records` / `datasets` すべて 0 件。**Phase 12 以降の開発中も fixture / synthetic / historical replay をこれらへ投入しない**。テストは rollback される transaction 内のみ。最初の1件は、実運用開始後に保存された情報から Outcome が確定した後にしか作らない。`test_the_teacher_tables_are_empty_too` が assert する | 2026-09-17 / 同上 | `workers/tests/test_db_model_lab.py` |
| D-164 | Phase 12 の全 view は 0 件を 0 件として出す | 20 surface すべて 0 行で出荷し、それをテストで assert する。合成値で埋めた dashboard は空の dashboard より悪い — 注意書きは、数値が実データに置き換わるずっと前に忘れられる。**fake accuracy / fake probability / synthetic champion を表示しない**。fold の score が無いときは `0.00` ではなく「not measured」（未測定の fold を悪い fold に見せない）。calibration が空の間は**どこにも確率を表示しない** | 2026-09-17 / Phase 12 実装 | `20260917290000` / `apps/web/src/app/model-lab/page.tsx` |
| D-165 | Route 別成績は `research.episode_attribution` に記録する（`thesis_key` から逆算しない） | `thesis_key` は現在 route を符号化しているが D-17a 未確定の暫定値。命名規約から成績表を作ると、規約が変わった瞬間に意味が黙って変わる。成績表示は常に **target / failure / unresolved の件数**で、率にしない（4 episode の勝率は勝率ではない）。unresolved を落とすと「測りやすかった事例だけの率」になる | 2026-09-17 / 同上 | 同上 / `apps/web/src/app/improvement/page.tsx` |
| D-166 | 空配列の `array_length` は NULL で、NULL の CHECK は通る | `promoted or array_length(refusal_reasons,1) >= 1` が、理由なしの refusal に対して NULL と評価されて**通っていた**（CI が捕捉）。`coalesce(..., 0)` で閉じた。D-146 の `case x when null` と**同じ NULL の罠**が1つ後の migration で再発しているので、Postgres の三値論理は書くたびに確認する | 2026-09-17 / 同上 | `20260917290000` |
| D-167 | 教師 observation の identity は kind ごとに別（security+date では無い） | `unique (security_id, as_of_date, label_version)` は「1銘柄1日1例」を意味し、これは誤り。同一銘柄・同一日に複数 setup・複数 thesis・複数 Episode が存在し得て、それらは入力も答えも違う**別の例**。partial unique index を3本にした: PREDICTED = `episode_id`、SETUP_NOT_ENTERED = `setup_id`、ELIGIBLE_ONLY = `security_id + as_of_date`。潰すと片方が黙って落ち、どちらが落ちるかは insert 順で決まる | 2026-09-17 / ChatGPT Phase 9-11 監査 | `20260917300000` / `workers/src/surge/labels/models.py` |
| D-168 | Teacher data は **input snapshot + decision + outcome** の3点で1件 | `labels.observation_contexts` に run 群 / feature version と snapshot / candidate・assessment 参照 / stage3_output_id / input_bundle_sha256 / coverage snapshot を保存する。Outcome だけ保存して後から入力を再構成すると、それは観測ではなく再構成で、**判断の後に届いた情報が静かに混ざる**。coverage 40% で作られた例と 100% で作られた例は別の例であり、これが無いと見分けがつかない | 2026-09-17 / 同上 | 同上 |
| D-169 | `PRICE_SUCCESS_EXOGENOUS` は label として残し、既定の学習 target からは外す | 「価格は上がったが、entry 時点の thesis がその成功を説明していない」という区別。predictive model の target に入れると、**自分の推論が予見していない結果の手柄を主張する**ことを学ぶ。`admission-1.1.0` を新しい DEFAULT とし、1.0.0 は SUPERSEDED として残す。DEFAULT policy が admit することを CHECK 制約で禁止し、SPECIAL_PURPOSE policy は理由を書けば採用できる | 2026-09-17 / 同上 | 同上 / `workers/src/surge/labels/admission.py` |
| D-170 | Promotion の閾値（200件等）は Canonical ではなく PROVISIONAL_POLICY | `research.promotion_policies` の versioned row にし、`provisional = true` をデータとして持つ。**根拠のある十分量ではない**。モジュール定数に埋めると、誰も推測だったことを覚えていない頃に「確定値」として読まれる。「200件に達したから学習可能」ではない。置き換える根拠は sample size / class balance / variance / walk-forward stability / calibration quality を実際に見ること。**challenger が惜しかったから緩める、はしない**。gate の判定は使った policy 名と provisional か否かを常に記録する | 2026-09-17 / 同上 | 同上 / `workers/src/surge/modellab/promotion.py` |
| D-171 | Local Production Runner は「動かなかった日」を記録する | Zero-Cost Core なので Production は本人の PC で始まる。有料 scheduler に無い固有の失敗モードは **PC が止まっていること**。`RUNTIME_OFFLINE` は schedule と attempt の差分から**事後に**書く（動いていない process は「動いていない」と書けない）。ノート PC を1週間閉じていた状態が「相場が静かだった1週間」と同じに見えてはいけない。lock と heartbeat は同一ファイル（heartbeat の無い lock は「実行中」と「落ちた」を区別できない）。Windows 固有にしない — Task Scheduler は CLI を叩くだけで、lock・heartbeat・retry は CLI 側が持つ | 2026-09-17 / D-104 | `workers/src/surge/runtime/runner.py` |
| D-172 | Readiness は market ごとに判定し、`PARTIAL_LIVE` は「実際に動いている capability」を要求する | 単一の全体 verdict は、最も詰まっている market に全体を合わせてしまい、片方がほぼ準備できている事実を隠す。`PARTIAL_LIVE` の条件を「gate でない check が PASS」にすると、**何も繋がっていない market が半分 live に見えた**（テストで発覚）。capability フラグを別に持ち、materials や price source のように**実データで動いているもの**だけが該当する。実行できなかった check は PASS ではなく FAIL として報告する（安全側） | 2026-09-17 / 監査 §12 | `workers/src/surge/runtime/readiness.py` |
| D-173 | **Alpaca Basic は US 価格源として不適格**（居住地条項とは別の理由） | Alpaca 自身の FAQ が「無料は IEX のみ、SIP は Algo Trader Plus」と明記。IEX は単一取引所で、同 FAQ の実例では AAPL 1日あたり IEX 12,630 約定 / 全取引所 535,134+ 約定。**Outcome のパス解決はセッションの高値・安値に対して順序を判定する**ので、IEX だけの高値では +20% 到達を系統的に取りこぼす。副次的効果として、ユーザー作業が1件**減った**: 「無料キーで `feed=sip` を1回叩く」は不要（公式が明記済み）。居住地の書面確認も Alpaca を採る場合にのみ必要 | 2026-09-17 / 一次情報で確認 | [live-activation-providers](research/live-activation-providers-2026-09-17.md) |
| D-174 | 実 Analysis Provider の判断軸は性能ではなく**入力の扱い** | Stage 3 の input bundle には Canonical v5.1（ユーザーの投資手法そのもの）が入る。Gemini API の**無料 tier は投稿内容を製品改善に使い、人間がレビューし得る**と規約に明記。Groq は Services Agreement で「Inputs / Outputs を学習・fine-tuning に使うことは許されない」と定め、既定で inference データを保持せず、ZDR も選べる（無料/有料の区別なし）。**第一候補は Groq 無料 tier**。残る確認は日本語品質・structured JSON・rate limit で、いずれも credential 取得後 | 2026-09-17 / 一次情報で確認 | 同上 |
| D-175 | D-06b は「入手不能」ではなく「口座契約が要る」 | 立花証券 e支店 API は公式に「利用料金 無料 0円」でリアルタイム株価・板を配信しており、0 円の候補が実在する。ただし**証券口座の開設**が前提（停止条件 1・2・3）。**以前の「3社とも蓄積禁止」を忘れていない** — 立花の API ページ自体に保存の記載は無く、条件は約款側にあるので、**約款の該当条項を読むまで採用しない**。必要なのは価格の取得ではなく **entry / audit の snapshot を合法に記録できること**で、蓄積を禁じる規約の下では価格が取れても Prediction は作れない（DB 制約が両価格の保存を要求する）。0 円候補が実在する以上、現時点で `ZERO_COST JP LIVE ENTRY UNAVAILABLE` とは**宣言しない** | 2026-09-17 / 再調査 | 同上 |
| D-176 | **D-103 は二つの問いだった。EOD と LIVE に分割する** | D-173 で「Alpaca Basic は不適格」としたのは **latest 系エンドポイントについてのみ正しかった**。同じ FAQ が historical について別のことを書いている: "For historical queries, the `end` parameter must be at least 15 minutes old to query SIP data without a subscription."。SIP は consolidated tape で、Outcome Engine が必要とする系列そのもの。**D-103-EOD**（consolidated 日次 OHLCV を 0 円で取れるか）は Alpaca delayed SIP で解ける見込み、**D-103-LIVE**（判断時点で取引可能だった価格）は未解決。15分遅れの系列は `entry_reference_price` になり得ず、Outcome 用と Entry 用が同一 provider である必要も無い | 2026-09-17 / ChatGPT Live Activation 監査 §1 | [alpaca-persistence-terms](research/alpaca-persistence-terms-2026-09-17.md) |
| D-177 | `feed=sip` は既定に頼らず、**リクエストとレスポンスの両方で**明示・検証する | 既定値は現在 `sip` だが、契約に応じて解決される種類の既定値は無料キーで静かに `iex` になり得る。さらに**レスポンスは別の事実**で、plan の downgrade や fallback が SIP の問い合わせに IEX を返し得る。`bars_url()` と `to_canonical_bars()` の両方で拒否する。加えて bar に `venue_basis` を持たせ、`assert_may_resolve_an_outcome()` が whole-market でない bar を Outcome パスへ渡すことを拒否する — **IEX の調査結果をメモではなく構造にした**。取りこぼした到達は「上がらなかった銘柄」と見分けがつかない | 2026-09-17 / 同上 §2 | `workers/src/surge/providers/alpaca_historical.py` |
| D-178 | Alpaca の**保存**条項は `NOT_SPECIFIED`。smoke までは進め、historical ingest は始めない | Terms 原文: "Content is provided exclusively for personal and noncommercial access and use. No part of the Service or Content may be copied, reproduced, republished, ... for publication or distribution or for any commercial enterprise"。market data は Content に明示的に含まれる。個人・非商用の利用は**明示的に許諾**され、禁止は公開・配布・商業という**目的に係る**。しかし private database という語はどこにも無い。**沈黙は同意ではない**ので `NOT_SPECIFIED`。`assert_smoke_sized()` が 5 銘柄 × 10 session を超える取得を拒否し、role binding は `enabled = false` で登録した（有効化が蓄積を始める行為そのもの）。`market.provider_license_policies` に **`private_persistence_allowed` 列を新設**した — 表示・再配布・商用にはそれぞれ列があったのに、**保存しているすべての行が依存する許諾にだけ列が無かった** | 2026-09-17 / 同上 §4 | `20260917310000` |
| D-179 | LLM provider の data policy は**データとして持ち、実行時に効かせる** | `InputUse` は 4 値（NOT_USED_FOR_TRAINING / USED_FOR_TRAINING / NOT_SPECIFIED / UNKNOWN）で、`NOT_USED_FOR_TRAINING` 以外には Canonical v5.1 を**送らない**。Gemini 無料 tier の policy も並べて持たせ、将来それを繋ごうとした経路が review ではなく例外で止まるようにした。ZDR は「available = true / enabled = None」— **有効かどうかはアカウントの事実で、このコードはアカウントを見たことがない**。false と書けばそれは主張になる | 2026-09-17 / 同上 §5・§7 | `workers/src/surge/analysis/groq_provider.py` |
| D-180 | Canonical を quota に合わせて削らない。`preflight()` に truncation 引数を**作らない** | v5.1 を削って無料枠へ押し込めば、以後すべての答えが**別の手法に対する答え**になり、出力のどこにもそれは現れない。超過時の正しい所見は `D-32_BLOCKED_FREE_QUOTA` である。token 見積りは CJK と Latin を分けて数える（`len/4` は日本語の canonical prompt を実寸の 1/4 に見積もる）。**アカウントの実 quota が未測定のうちは preflight を通さない** — ドキュメント記載値で通しておくと、通らなくなる瞬間まで通り続ける | 2026-09-17 / 同上 §8 | 同上 |
| D-181 | **Intraday Entry Analysis を実装した。ENTRY はこの contract だけが出せる** | Stage 3 に ENTRY が無いのは正しく、かつ十分ではなかった — TRIGGER_HIT の先に、実価格を見て「今この価格で入れるか」を答えるものが無かった。5 状態（ENTRY / WATCH_BREAKOUT / WATCH_PULLBACK / WATCH_OTHER / REJECT）。**setup 2 状態は intraday contract から除外**（終わった session についての主張を session の中から上書きさせない）。Stage 3 validator が拒否する「enter now」系の文言は**ここでは許す**。model は判断した価格を echo させ、bundle の価格と一致しなければ拒否する。`TRIGGER_HIT → IN_REANALYSIS →（model）→ 検証 → decide()` を 1 本に繋ぎ、**IN_REANALYSIS は model 呼び出しの前に**書く（途中で落ちたとき「analysis 中だった」と残る） | 2026-09-17 / 同上 §9 | `workers/src/surge/analysis/entry_analysis.py` / `workers/src/surge/jobs/entry_analysis_job.py` |
| D-182 | Entry validator の所見は **2 種類**に分ける。片方だけが handover を止める | `errors` = 答えが壊れている（failure line 無し、echo 価格の不一致、zone が threshold と同一、stand-in からの ENTRY）。これは decide() へ渡さない — **壊れた ENTRY を REJECT として記録すると、analysis がしていない判定が台帳に載る**。`system_refusals` = 答えは整っているがシステムが実行しない（universe 外、3,000円超）。これは**銘柄についての所見**であり、entry ledger の 7 status のうち 6 つはそのために存在する。止めると hit rate の分母が黙って狂う。coverage だけは**前提条件**として model 呼び出しの前に確認し、満たさなければ analysis を走らせない（trigger は hit のまま残る） | 2026-09-17 / 同上 §10 | 同上 |
| D-183 | 3,000円 Hard Filter の権限は model にも validator にも無い | model には規則を伝える（違反が「知らなかった」ではなく「判断」として見えるため）。validator は**報告するだけ**で handover を止めない。実際に拒否するのは `decide()` と DB 制約の 2 箇所。テストは validator を迂回しても over-limit の Prediction が作れないことを assert する | 2026-09-17 / 同上 §10 | `workers/tests/test_entry_analysis.py` |
| D-184 | Production training dataset は、**入力側が記録されていない label を admit できない** | `labels.observation_contexts` は作ったが、admission が存在を要求していなかった。要求する最低限: coverage_snapshot / feature_version（または**その不在の理由**）/ production・universe・market_data の run / kind ごとの decision 参照（PREDICTED = episode + attempt + stage3 か bundle hash、SETUP_NOT_ENTERED = setup、ELIGIBLE_ONLY = 追加要求なし）。**ELIGIBLE_ONLY に episode を要求しない** — 要求すれば「誰も拾わなかった銘柄」の全体が落ち、それは見逃しを教える部分そのもの。**lineage 不足の label は保存してよい**（outcome は起きた。落とせば記録が偏る）が admit されない。Python と DB trigger の両方で守り、`purpose` の既定は `PRODUCTION_TRAINING`（何も言わない dataset を「学習に使う」側に倒す） | 2026-09-17 / 同上 §11 | `20260917310000` / `workers/src/surge/labels/admission.py` |
| D-185 | **D-06b の判定を `PROHIBITED / NEEDS_WRITTEN_EXCEPTION` へ変更する**（D-175 を訂正） | D-175 は「約款を読むまで保留」としたが、**公式ページに既に明示の禁止がある**。原文: 「７．当社提供情報は、蓄積、編集および加工等は禁止しております。」 対象は「当社提供情報」でニュースに限られない。一方、同項が参照するインターネット取引規定 第18条に「蓄積」の語は無く、禁止は第三者提供・営業使用・第三者提供目的の加工・本人の証券投資以外の目的という**目的による限定**の形をとる。**二つの文書が食い違っており、厳しい方を採る**。書面照会を作成した。**口座開設を先に要求しない**（順序が逆で、口座開設自体が停止条件 1）。3 社すべてが不可と確定して初めて `ZERO_COST JP LIVE ENTRY UNAVAILABLE` を宣言する | 2026-09-17 / 同上 §12・一次情報で確認 | [tachibana-api-accumulation-enquiry](research/tachibana-api-accumulation-enquiry.md) |
| D-186 | Analysis gate を **EOD と ENTRY に分ける**。Stage 3 が繋がっただけで LIVE_READY にしない | Stage 3 は ENTRY 状態を持たず閉場後に走るので、そこに model を繋いでも作れるのは setup であって Prediction ではない。readiness は `eod_analysis_provider` と `entry_analysis_provider` を別 check にし、両方 PASS して初めて analysis gate を通る。同様に価格も `eod_price_provider` と `intraday_entry_price_provider` に分けた | 2026-09-17 / 同上 §14 | `workers/src/surge/runtime/readiness.py` |
| D-187 | gap 検出は **install marker より前を outage と数えない** | 新しい機械では log が空なので、floor が無いと lookback 期間の全 occurrence を outage と報告し、**この system が存在しなかった日に `RUNTIME_OFFLINE` 行を書く**。それらは後から本物の停止と見分けがつかず、offline 記録が防ぐはずだった混同そのものになる。`runner_cli init` が marker を 1 度だけ書き、**前へ動かさない**（動かすことは停止を記録するのではなく消すこと）。marker が無ければ `gaps` は拒否する | 2026-09-17 / D-104 の実装中に発覚 | `workers/src/surge/jobs/runner_cli.py` |
| D-188 | **`D-32` は id が衝突している**（記録のみ。勝手に振り直さない） | このログの `D-32` は `price_obstacles.status` を WEAKENED / INVALIDATED にする基準（Phase 7 前）。一方、Live Activation 監査以降の会話と readiness report は `D-32` を「実 Analysis Provider」の意味で使っている。readiness の blocker id は監査の語彙に合わせて `D-32-EOD` / `D-32-ENTRY` とした（文字列としては別物なので曖昧ではない）。**勝手に振り直すと監査側の語彙と食い違う**ので、ここに衝突を記録するに留める。振り直すならユーザーの指示で行う | 2026-09-17 / 実装中に発見 | このログ B-3 の `D-32` 行 |
| D-189 | **Real EOD / Stage 3 Analysis Provider**（新しい id） | `D-32` の衝突を解消するための正式な採番。Stage 3 は setup と watch を作り、ENTRY は作らない。readiness の blocker id は `D-32-EOD` から **`D-189`** へ変更。既存の `D-32`（`price_obstacles.status` の基準）はそのまま保持し、D-188 は衝突発見の記録として残す | 2026-09-17 / ChatGPT Live Activation 監査 2 | `workers/src/surge/runtime/readiness.py` |
| D-190 | **Real Intraday Entry / Reanalysis Provider**（新しい id） | 同上。**正式 Prediction はこの contract からしか生まれない**ので、D-189 が PASS しても analysis gate は通らない。readiness の blocker id は `D-32-ENTRY` から **`D-190`** へ変更 | 2026-09-17 / 同上 | 同上 |
| D-191 | `D-32_BLOCKED_FREE_QUOTA` は decision id として廃止し、runtime reason code `ANALYSIS_FREE_QUOTA_BLOCKED` にする | decision id は「人が決めること」、reason code は「実行時に起きたこと」で、別の語彙である。混ぜると、quota に当たった実行ログが未決事項一覧に見えてしまう。`Preflight.decision_id` も `reason_code` へ改名した | 2026-09-17 / 同上 | `workers/src/surge/analysis/groq_provider.py` |
| D-192 | **private persistence を LicenseAction と LicensePolicy の第一級の項目にし、ingest orchestration で強制する** | `assert_smoke_sized()` は caller が呼ばなければ効かない「規律」でしかなかった。`MarketIngestJob` に `PersistenceDecision` を**デフォルトなしの必須引数**として持たせ、object store・raw_objects・parquet・daily_bars・coverage へ書く**前に**判定する。`ALLOWED` 以外（PROHIBITED / NOT_SPECIFIED / UNKNOWN）はすべて durable write を拒否する。規約を読んでいない caller は `PersistenceDecision.unknown()` を渡すことになり、それは止まる — **知らないことの正しい結果は「書かない」** | 2026-09-17 / 同上 §1 | `workers/src/surge/licensing.py` / `workers/src/surge/market/ingest.py` |
| D-193 | **Alpaca の feed は「requested」であって「verified」ではない**（前回の記述を訂正） | 前回 “response でも SIP を確認している” と書いたが、**確認していたのは caller の引数だけ**だった。Alpaca の bars response には feed 識別子が無く、確認する対象が存在しない。根拠は「明示的な `feed=sip` を送った」「HTTP 200 が返った」「公開 API 契約がその意味を定めている」の 3 点であり、これは信じるに足る根拠であって検証ではない。`venue_basis` を `CONSOLIDATED_SIP_REQUESTED` と `CONSOLIDATED_SIP_VERIFIED` に分け、requested_feed を verbatim で保存する。**response_verified_feed のようなフラグは作らない** | 2026-09-17 / 同上 §2 | `workers/src/surge/market/models.py` / `providers/alpaca_historical.py` |
| D-194 | credential smoke は **fetch → validate → report → discard**。保存 0 件 | `credential_smoke()` は bar を返さず `SmokeReport`（件数・日付・ハッシュ・ラベルのみ、価格は 1 つも含まない）を返す。bar を返せば「捨てること」が caller の規律になり、規律は guard ではない | 2026-09-17 / 同上 §1 | `providers/alpaca_historical.py` |
| D-195 | Groq の retention は ZDR 未確認の間 `TRANSIENT_FOR_ABUSE_AND_RELIABILITY`、privacy gate は **ZDR が ON と確認できた場合のみ** PASS | 「既定では保持しない」と「信頼性・不正対応のため一時的に保持し得る」は同じページに併記されている。確認が取れていない間は**弱い方が真**。ZDR が available なのは製品の事実で、**アカウントの事実ではない**。terms_url も `console.groq.com/docs/legal/services-agreement` に訂正 | 2026-09-17 / 同上 §3 | `workers/src/surge/analysis/groq_provider.py` |
| D-196 | **quota 未測定では Canonical を送らない。`quota_probe()` を別経路として設ける** | **実際に穴だった**: quota unknown → `preflight` が `fits=False` を返す → しかし decision_id が無いため `_call()` の分岐を素通りし、**そのまま送信していた**。Canonical v5.1 が、送れるかどうか誰も知らないうちに出て行く経路だった。修正: 未測定は `QuotaUnknown` で拒否。`quota_probe()` は canonical も addenda も実データも含まない極小 prompt を 1 回だけ送り、rate-limit header から実値を読む（`x-ratelimit-limit-requests` は**日次**、`x-ratelimit-limit-tokens` は**分次**という非対称に注意） | 2026-09-17 / 同上 §4 | 同上 |
| D-197 | **Groq 無料 tier の feasibility（実測）**: 最小リクエストは約 13,800 token | Canonical v5.1（32,012 bytes）だけで約 9,900 token、addenda 4 本を足して約 13,200、contract を足した**最小の Stage 3 / Entry リクエスト**が 13,503 / 13,784 token（データ 0 件の floor）。Groq の公開 free 制限は **`openai/gpt-oss-*` が 8,000 TPM** なので、**1 リクエストすら送れない** → `ANALYSIS_FREE_QUOTA_BLOCKED`。ただし **`groq/compound*` は 70,000 TPM** で size 上は収まる（RPD 250）。**「Groq 無料は不可」と一般化しない** — これは model family ごとの制限であり、provider 全体の制限ではない（D-176 と同じ誤りを繰り返さない）。compound が strict structured outputs に対応するかは**未確認**で、実測するまで採用しない | 2026-09-17 / 同上 §5・実測 | `workers/src/surge/jobs/analysis_feasibility.py` |
| D-198 | Alpaca の保存可否は書面照会する | 照会文を作成（本人1名 / private・noncommercial / 第三者提供なし / 公開なし、保持期間と削除条件、Basic と Pro の差、Basic に取引所 subscriber agreement が及ぶか）。回答前は **credential smoke 可・historical accumulation 不可**、これは D-192 の guard で構造的に担保されている | 2026-09-17 / 同上 §6 | [alpaca-persistence-enquiry](research/alpaca-persistence-enquiry.md) |
| D-199 | **D-104 の実装は完了。Task 登録は「1 市場が繋がってから」まで行わない** | runner・CLI・PowerShell script は完成しており、残るのはユーザーの機械へのタスク登録だけ。ただし**今は依頼しない**: price provider も analysis provider も未接続なので、登録しても毎日空の pipeline が起動するだけで、run log には失敗行だけが積み上がる。**価値が無いばかりか、log を読む習慣を先に壊す**。1 市場分の price + analysis が繋がった時点で登録する | 2026-09-17 / 同上 §7 | `ops/windows/README.md` |
| D-200 | Readiness CLI は **DB へ実際に接続する**。「接続文字列がある」ことを確認するのは質問したことにならない | 以前は `SURGE_DATABASE_URL` の有無だけを見て `collect()` を呼んでいなかったので、report は**与えられた flag の説明**でしかなかった。psycopg2 で **read-only session** として接続し market ごとに `collect()` する。読むのは security master freshness / authoritative universe / teacher rows / mock guard / live material source / **provider role bindings** / **実 market data 行数** / **実 FX 行数**。実行できなかった query は `database_check_failed` として**失敗扱いで報告**する（黙って False を返すと本物の否定と区別がつかない） | 2026-09-17 / ChatGPT Live Activation 監査 3 §A | `workers/src/surge/jobs/readiness_cli.py` / `workers/tests/test_db_readiness.py` |
| D-201 | **BOUND と LIVE_OBSERVED を分ける。** `Liveness` は3値 | Cloud 実測で `FX_USDJPY = ecb` は **enabled かつ binding 済み**、しかし `market.fx_rates = 0`。これを「FX source あり」と報告すれば嘘になり、「FX source なし」と報告すれば**最初の1回の取得以外は全部終わっている**という事実を捨てる。`NOT_BOUND` / `BOUND_NOT_LIVE_OBSERVED` / `LIVE_OBSERVED` を分け、次にやることが「決めること」なのか「1回走らせること」なのかを report が言い分ける。`market.daily_bars` も同様（現在 0 行） | 2026-09-17 / 同上 §A | `workers/src/surge/runtime/readiness.py` |
| D-202 | **Entry Analysis を DB-backed にする。「crash でも stored state が IN_REANALYSIS」は実際には false だった** | 以前の job は Python object を動かしていただけで、何も保存していなかった。process が trigger と answer の間で死ぬと watch は `TRIGGER_HIT` のまま残り、それは「この trigger には何も起きていない」と読める — ordering が防ぐはずだった混同そのもの。`prod.entry_analysis_executions` を作り、**model call の前に** watch transition と execution row を同一 transaction で書く。`begin_entry_analysis()` は `TRIGGER_HIT -> IN_REANALYSIS` と STARTED 行を一緒に作り、worker には table への INSERT/UPDATE/DELETE を与えない | 2026-09-17 / 同上 §B | `20260917330000` / `workers/src/surge/analysis/execution.py` |
| D-203 | recovery は `IN_REANALYSIS` を拾う。`TRIGGER_HIT` だけを見る runner では**crash した案件を必ず取りこぼす** | crash するとまさに watch が1つ先へ進んでいるので、`TRIGGER_HIT` の sweep は落ちたものだけを見逃す。`ui.entry_analysis_in_flight` を正本にし、`has_a_stored_answer` で「model にもう払ったか」を分ける | 2026-09-17 / 同上 §B | 同上 |
| D-204 | idempotency key は **(watch_id, trigger_transition_id, analysis_kind)** | watch だけを key にすると、正当に REARM して再 trigger した watch の**2回目の分析が1回目の重複として拒否**される。3つの crash case を fixture と DB integration test で固定: (1) STARTED commit 後の死 → recovery が拾える、(2) answer 保存後の死 → **同じ answer から再開**（2度払いを避けるだけでなく、2回目の call が違う答えを返したら「この trigger の分析」がどちらか決まらなくなる）、(3) prediction commit 後の死 → **2本目を作らない** | 2026-09-17 / 同上 §B | `workers/tests/test_entry_analysis_execution.py` / `test_db_entry_analysis_execution.py` |
| D-205 | idempotency の確認は **watch 状態の precondition より前**に行う（実装中に見つけた順序バグ） | 「reanalysis は TRIGGER_HIT から」を先に検査すると、**crash 自身が作った状態**を理由にあらゆる recovery が拒否される。store があるときは記録を先に読み、新規作成のときだけ TRIGGER_HIT を要求する | 2026-09-17 / 同上 §B | `workers/src/surge/jobs/entry_analysis_job.py` |
| D-206 | execution row の hash 3つは **write-once**（immutable ではない） | prompt / bundle / canonical hash は answer と一緒に入るので、「作成時から不変」にすると**記録そのものの update が拒否される**。null → 値は許し、値 → 別の値は拒否する。後者を許すと、保存済みの answer を別の prompt に付け替えられる | 2026-09-17 / 実装中に発覚 | `20260917330000` |
| D-207 | **GPT-OSS の token 数は exact で測れる**（credential 不要）。`o200k_harmony` は OpenAI が公開している | 実測: Canonical v5.1 のみ **9,836**、+addenda **13,294**、最小 Stage 3 **13,570**、最小 Entry **13,772**（データ 0 件の floor）。従来の推定値との差は **1% 未満**で、推定が tokeniser 非公開の provider に対しても信頼できる根拠になった。`Count` は estimate と exact を**両方持つ**。tiktoken が無い環境では exact を `None` にして「推定である」と言う — exact と書いたものは exact でなければならない | 2026-09-17 / ChatGPT 監査 3 §C | `workers/src/surge/analysis/tokenizer.py` |
| D-208 | TPM 判定に **completion reserve** を含める。combined meter と ITPM/OTPM は別扱い | Groq 無料枠は combined な tokens-per-minute なので、model の出力も prompt と同じ allowance から引かれる。input だけを見た preflight は **API が拒否する request を承認**する。実測: 13,772 + reserve 2,048 = **15,832** 対 `openai/gpt-oss-*` の **8,000 TPM** → 1 request も送れない。account が ITPM/OTPM を別に持つ場合はそれぞれ別に判定する | 2026-09-17 / 同上 §C | `workers/src/surge/analysis/groq_provider.py` |
| D-209 | `groq/compound*` は「strict 未確認」ではなく **`JSON_SCHEMA_UNSUPPORTED_BY_CURRENT_DOCS`** | Groq の structured-outputs ページは**肯定リスト**なので、載っていない family は「文書化された不在」であって未解決の問いではない。`OutputMode` を4値（STRICT_JSON_SCHEMA / BEST_EFFORT_JSON_SCHEMA / JSON_OBJECT / PLAIN_TEXT）にし、GPT-OSS = STRICT、Compound = JSON_OBJECT。Compound 経路は `{"type":"json_object"}` → parse → **こちら側で schema 検査** → 不正なら bounded retry（3回）→ 尽きたら analysis FAILED で、**formal decision へは進めない**。schema を provider が守ったかどうかに関わらず**契約はこちら側で検証する** | 2026-09-17 / 同上 §C | 同上 |
| D-210 | `quota_probe()` は **Structured Outputs を使わない** | probe は header を読むためのもので、**まさに調べようとしている capability に依存させると**、未文書の model で失敗してその model の limit について何も分からなくなる。`response_format` を一切送らず、canonical も addenda も実データも含まない極小 prompt を1回だけ送る | 2026-09-17 / 同上 §C | 同上 |
| D-211 | `PersistenceDecision` は **provider / dataset / policy_version に束縛**する | `permission = ALLOWED` だけでは「licence の形をしたもの」であって**この data の licence ではない**。ECB の decision は本物で正しく、そして Alpaca の fetch を保存してよい根拠ではない — 束縛が無ければ、**実在する許諾が、それが書かれた対象ではない dataset の audit trail に載る**。construction 時に provider/dataset を、durable write 時に `policy_version` も照合する。**どちらかが NULL なら wildcard ではなく mismatch**（「どの規約が適用されたか記録していない」は「同じ規約だった」の証拠ではない）。標準の入口は `PersistenceDecision.from_license_policy()` | 2026-09-17 / 同上 §D | `workers/src/surge/licensing.py` / `market/ingest.py` |
| D-212 | **Production は 3 transaction に分ける。durability は commit 位置で決まる** | `DatabaseExecutionStore` は commit しないと明記してあったので、「model call の前に記録がある」は**未 commit の snapshot について真**でしかなく、そこで殺された process は行を残さなかった。TX1（trigger 検証 → IN_REANALYSIS → STARTED → commit）の**後にだけ**外部 API を呼ぶ。TX2（answer + hash → commit）。TX3（validation → decide → attempt → episode → prediction → watch 遷移 → COMPLETE → commit）。**TX3 は 1 つ**にする — prediction の insert が失敗したら attempt も episode も watch 遷移も completion も巻き戻らなければ、「判断の無い主張」が残る | 2026-09-17 / ChatGPT 監査 4 §A | `workers/src/surge/jobs/production_entry_analysis.py` |
| D-213 | **Formal Entry decision を実際に DB へ書く** | `EntryAnalysisJob` は `decide()` を Python 上で呼ぶだけで `write_attempt` / `write_episode` / `write_prediction` を呼んでいなかった。Python の中にしか無い attempt は台帳ではない。execution の `entry_attempt_id` / `prediction_id` には **DB が返した id** を入れる（`getattr(dataclass, "attempt_id", None)` は禁止 — dataclass の自称 id は INSERT が答えるまで推測である） | 2026-09-17 / 同上 §B | 同上 / `workers/tests/test_db_production_entry_analysis.py` |
| D-214 | `begin_entry_analysis` は **watch を FOR UPDATE して因果を検証**し、security は導出する | caller が watch_id / trigger_transition_id / security_id を別々に渡して全部信じられていた。**別 watch の trigger**・**消化済みの古い trigger**・**watch のものでない security** がすべて通った。修正: watch を lock、transition が (a) その watch のもので (b) `to_state = TRIGGER_HIT` で (c) **現在の trigger**（より新しい TRIGGER_HIT が無い）ことを要求、watch 自身も `TRIGGER_HIT` であること、security は `prod.watches` から**導出**し引数があれば一致を要求 | 2026-09-17 / 同上 §C | `20260917350000` |
| D-215 | Stored answer は **lossless**。特に `reachable_zone_basis_kinds` と `reachable_zone_basis` を落とさない | この2つは**保存されていなかった**のに、entry validator は ENTRY に対してこれらを要求する。つまり crash 後に resume すると、**元の answer が通した契約を再構成した answer が通せない**。しかも失敗の見え方は「model が気を変えた」に似ていて原因に辿り着けない。全 semantic field に列を与え、raw response も保存する。roundtrip fixture で semantic equality を固定 | 2026-09-17 / 同上 §D | 同上 / `execution_db.py` |
| D-216 | prompt / bundle / canonical hash は **保存が必須**（HOSTED_LLM では completion の前提条件） | 列はあったのに `record_answer` / `complete` は null を渡していた。どの prompt・どの bundle に対する answer かは再現性そのもの（CLAUDE.md 1-18）。`record_entry_analysis_answer` は hash 無しの呼び出しを拒否し、`complete_entry_analysis` は HOSTED_LLM に対して 4 つの hash を要求する（stand-in は decision へ進まないので除外） | 2026-09-17 / 同上 §E | 同上 |
| D-217 | **transient failure と terminal failure を分ける。FAILED は watch を IN_REANALYSIS に放置できない** | **実際に穴だった**: provider timeout → execution FAILED → watch は IN_REANALYSIS のまま。recovery は finished execution を見ず、**他に watch を IN_REANALYSIS から動かす経路が無い**ので、ネットワークの瞬断が銘柄を恒久的に system から消していた。`RETRY_PENDING` を追加し、transient（PROVIDER_ERROR / TIMEOUT / RATE_LIMITED）は execution を開いたまま同一 execution を再試行する。terminal は `fail_entry_analysis` が **watch が IN_REANALYSIS ならその場で拒否**する（REARMED / REJECTED / EXPIRED へ動かしてから呼べ）。**未知の failure は terminal 扱い** — 分からないものを retry 可能と仮定すると恒久障害が無限ループになる | 2026-09-17 / 同上 §F | 同上 |
| D-218 | Readiness の LIVE は **freshness 付き**。`count(*) > 0` を live と呼ばない | 行があることは pipeline が動いていることではない。判定は **bound provider の行だけ**で行い（別 provider の古い行が現 provider を live に見せない）、最新 `available_at` の古さで `LIVE_FRESH` / `LIVE_STALE` を分ける。上限 3 日（週末＋祝日1日を許し、それ以上は止まっている）。**日付を持たない行は fresh と見なさない**（「どれだけ古いか分からない」は「新しい」の証拠ではない）。`LIVE_READY` は `LIVE_FRESH` だけを使う | 2026-09-17 / 同上 §G | `workers/src/surge/runtime/readiness.py` |
| D-219 | **Groq Compound の built-in tools を全面禁止する** | compound / compound-mini は web search・visit website・code execution・Wolfram Alpha が**既定で有効**。分析中にこれが動くと (1) `decision_cutoff_at` より後の情報が入り（1-7 / 1-16 が存在する理由そのもの）、(2) 教師データが system の知らなかった事実で汚染され、(3) bundle にも hash にも無い source に基づく判断になり再現・監査が不能になり（1-18）、(4) zero-cost の外で課金される。request に `tool_choice = "none"` と `compound_custom.tools.enabled_tools = []` を付ける。**ただし Groq は compound に対するこれらを文書化していないので request 側は best effort**。**強制するのは response 側**: `executed_tools` が1件でもあれば `ANALYSIS_EXTERNAL_TOOL_USED` として analysis FAILED とし、その output は Stage 3 setup にも Entry decision にも Teacher input にも使わない。フィールドの置き場所は断定できないので top-level / choice / message の3箇所を見る | 2026-09-17 / 同上 §H | `workers/src/surge/analysis/groq_provider.py` |
| D-220 | o200k_harmony で測った値は **prompt content tokens** であって total request tokens ではない | chat API の role framing・schema・harmony control token は含まれない。`Count` は content-only であることを名前と docstring で明示する。**結論は変わらない**: Entry prompt content 13,772 > GPT-OSS free TPM 8,000 で、framing を足せば差はさらに開く | 2026-09-17 / 同上 §I | `workers/src/surge/analysis/tokenizer.py` |
| D-221 | **migration file と DB の一致をテストで守る**（3回 drift したので） | `supabase/migrations/` が正本というのは意図の宣言で、意図は自動では守られない。file からではなく貼り付けから apply すると、**挙動は同じで本文が違う**状態になり、file を読む人は DB が実行していないものを読む。実際に **4 関数で発生**（`begin_entry_analysis` / `complete_entry_analysis` / `labels.lineage_gaps` / `labels.assert_admitted_member_has_lineage`、いずれも貼り付け時に `--:` コメントを削ったため）。`test_db_migrations_match.py` が全 83 関数の本文を空白正規化して比較する。**直すのは DB 側**（file が正本） | 2026-09-17 / 実装中に発覚（3回目） | `workers/tests/test_db_migrations_match.py` |
| D-222 | commit するテストは **使い捨て DB でしか走らない** | TX 境界のテストは rollback できない（「commit 後に何が残るか」が検査対象）。`SURGE_TEST_DATABASE_URL` が production を指していた場合、合成 prediction が教師母集団に入り、後から本物と見分けがつかなくなる。教師テーブルに1行でもあれば **skip する**（実行後に気づいても取り消せないので、事前に止める）。自分が作った行は後始末する | 2026-09-17 / 同上 | `workers/tests/test_db_production_entry_analysis.py` |
| D-223 | **D-221 の訂正: CI の drift test は cloud を見ていない。実測は CLI で行う** | D-221 は「test が一致を守る」と書いたが、CI job は `supabase/migrations/` を空の Postgres へ適用し、**その DB を同じ file と比べていた**。これは恒真に近く、捕まえられるのは parser のバグだけで、**規則が防ごうとしている cloud の乖離は一度も見ていなかった**（CI は cloud へ接続しない）。実際その parser バグが1件あり（`pipeline.snapshot_securities` は signature 変更のため `create function` で再定義されており、`create or replace` だけを見る regex に映らず、**正しい本文を持つ DB を drift と報告していた**）。比較を `surge.runtime.schema_drift` へ出し、`python -m surge.jobs.schema_drift_cli` で任意の DSN に向けられるようにした。cloud に対して実行した結果、**10 関数が drift**（すべて `--` コメント欠落。挙動は同一なので他の test では永久に見えない）、**1 関数が unmanaged**（`ref.tmp_trigger_probe` = `select 1` の残骸）。全件を file から再適用して md5 一致を確認し、probe は migration で drop した。**cloud DSN を CI Secrets に置く案は採らない**（PR ビルドすべてに production への経路を与えるため） | 2026-09-18 / 実装中に発覚（4回目） | `workers/src/surge/runtime/schema_drift.py` / `workers/src/surge/jobs/schema_drift_cli.py` / `docs/ops/schema-drift-check.md` |
| D-224 | readiness の `migrations_in_sync` は **flag ではなく実測**にする | `--migrations-in-sync` は caller の申告だった。Audit 3 §A で「binding があること」と「実データがあること」を分けたのと同じ理由で、「一致していると言うこと」は一致ではない。`collect()` が接続越しに `compare()` を呼び、drift した関数名を detail に載せる。**接続が無い場合は PASS ではなく「誰も見ていない」**。flag は削除した（残すと申告経路が残る） | 2026-09-18 / 同上 | `workers/src/surge/runtime/readiness.py`（`readiness-1.5.0`） |
| D-225 | drift check は **drifted と unmanaged を分ける**。「どの file にも無い」は本文比較をすり抜ける | 本文が違う関数は比較で落ちるが、**どの migration にも定義が無い関数は比較対象にすらならない**ので、手作業で作られた object は永久に無傷で残る。`ref.tmp_trigger_probe` がそれだった。さらに `examined = 0` を in_sync と呼ばない（クエリが何も返さなかった状態と、全件一致した状態は見た目が同じ） | 2026-09-18 / 同上 | 同上 |
| D-226 | **transient failure に予算を付ける**。無制限 retry は stranded watch を作り直す | D-217 で「timeout は FAILED にせず retry」としたが、**retry 回数にも時間にも上限が無かった**。provider が午後いっぱい落ちていれば各試行は transient で、状態は permanent — watch は IN_REANALYSIS のまま何時間も座り、**直したはずの穴がゆっくり再現する**。`MAX_TRANSIENT_ATTEMPTS = 5` と `ANALYSIS_DEADLINE = 30min` を置き、尽きたら `RETRY_EXHAUSTED` / `ANALYSIS_DEADLINE_PASSED`（どちらも terminal）へ昇格させ、**watch を REARMED へ動かしてから** fail する（DB guard があるので忘れられない）。trigger は未回答のまま watch が再武装する — 答えなかった provider から判断を作るより正しい | 2026-09-18 / 監査4後の自己再点検 | `workers/src/surge/analysis/execution.py` / `jobs/production_entry_analysis.py` |
| D-227 | deadline は **`decision_cutoff_at` から測る**（`started_at` からではない） | `started_at` は `clock_timestamp()`＝行を書いた server の時計で、判断の時計ではない。問われているのは「入力がどれだけ古いか」なので、凍結した時刻から測るのが正しい。insert がどれだけ速くても、cutoff の40分後に返ってきた答えは40分前の価格についての答えである。replay でも同じ判定になる | 2026-09-18 / 同上 | 同上 |
| D-228 | recovery は **resume できなかったものを報告する**。`continue` で飛ばさない | `resume_all` は bundle の無い in-flight execution を黙って skip していた。skip された execution は **IN_REANALYSIS のままの watch** であり、他に動かすものは無い。「詰まっていない」と「詰まっていたが素通りした」が同じ空の結果になっていた。`Recovery{resumed, unresumable}` を返し、各 unresumable に watch_id と理由を載せる | 2026-09-18 / 同上 | `workers/src/surge/jobs/production_entry_analysis.py` |
| D-229 | **provider の失敗は例外自身が種類を持つ。catch した場所で決めない** | `_tx2` は provider からの例外を**すべて transient**として retry していた。つまり `ExternalToolUsed`（bundle の外へ出た）・`FreeQuotaExceeded`・`CredentialsMissing`・`InputPolicyViolation` まで再送していた。**外部 tool の件は積極的に有害** — retry はもう一度呼ぶことであり、もう一度外へ出る機会と課金を意味し、その答えはどちらにせよ使えない。`ProviderFailure` 基底クラスに `failure_class` を持たせ、Groq の各例外が自分の分類を宣言する（**名前照合にしない** — rename で terminal が黙って retry に変わる）。既定は **terminal**（D-217 の既定の逆）。terminal が watch を REARMED へ動かすようになった今、誤判定の代償は「trigger 1件を取り逃す」であり、逆向きの誤判定は「5回の呼び出しと30分」あるいは禁止事項そのもの | 2026-09-18 / 監査4後の自己再点検 | `workers/src/surge/analysis/execution.py` / `analysis/groq_provider.py` |
| D-230 | readiness は **IN_REANALYSIS で止まっている analysis を報告する** | watch machine が IN_REANALYSIS から出る道は「来なかった判断」の1本だけなので、開いたままの execution はその銘柄を無期限に押さえる。recovery pass は存在するが**実行されなければ意味が無く**、intraday price source が決まるまで intraday schedule 自体が無い（D-103-LIVE / D-06b）。よって runner が無くても見えるよう readiness 側で数える。**開いていること自体は問題ではない**（1分前に始まったものは進行中）。deadline を過ぎたものだけを FAIL にする | 2026-09-18 / 同上 | `workers/src/surge/runtime/readiness.py`（`readiness-1.6.0`） |
| D-231 | **Intraday input は TX1 で durable に固定する**。recovery は保存された入力から再開する | execution row は「analysis が始まった」ことと「何を答えたか」を持っていたが、**何を聞いたか**を持っていなかった。bundle・guard facts・thesis は caller の memory にあり、`resume_all` はそれを引数で受け取っていた。つまり durability は**落ちた process が生きている間だけ**成立していた。新しい bundle を作って再開すれば、20分後の intraday bundle は別の価格・別の tape であり、**元の trigger の名で別の問いに答える**ことになる。TX1 で prompt を render して hash を取り、入力全体（bundle_serialized・decision price/currency/observed_at・FX・universe・coverage・thesis・setup_ids・4 hash・input_version）を **送信前に commit** する。recovery は保存入力から prompt を再構築し、**prompt_sha256 の完全一致を要求**する。不一致は `INPUT_RECONSTRUCTION_MISMATCH`（terminal、watch は REARMED）。`resume_all` は bundle を一切受け取らない。canonical 本文は複製せず hash + ファイル同一性で持つ | 2026-09-18 / ChatGPT 監査 5 §A | `supabase/migrations/20260918010000_durable_input_snapshot.sql` / `workers/src/surge/analysis/input_snapshot.py` |
| D-232 | bundle_serialized は **jsonb ではなく text** で持つ | jsonb は key を並べ替えるので、読み戻した値から `bundle_sha256` が再現しない。hash の対象と保存形式を一致させるため byte 単位で保存する | 2026-09-18 / 同上 | 同上 |
| D-233 | 保存入力の hash は **write-once から immutable へ** | D-216 で hash は answer と一緒に届くので write-once（null→値）にした。今は request と一緒に TX1 で届くので、以後の変更はすべて**聞いた内容の書き換え**になる。`record_entry_analysis_answer` は hash を受け取るが**書かずに照合**し、TX1 と違えば拒否する（start と ask の間に別の prompt を render した runner が、開始時の記録を上書きするのを防ぐ） | 2026-09-18 / 同上 | 同上 |
| D-234 | **`decision_completed_at` は runner の clock で決める**。caller から受け取らない | `EntryGuardFacts` が model call の前から `decision_completed_at` を持っていた。つまり caller が**判断が始まる前に判断の終了時刻を宣言**していた。entry price は「その時刻より後に観測されたもの」だけが使えるので、caller が**どの価格が数えられるかの線を動かせた**。answer 受信 + semantic validation 完了の時点で runner が自分の clock で決め、`analysis_answered_at` と共に execution に保存する。一度書いたら動かせない（guard） | 2026-09-18 / 同上 §B | `workers/src/surge/analysis/entry_analysis.py` / `jobs/production_entry_analysis.py` |
| D-235 | **entry_reference_price は判断完了後に `EntryPriceObserver` で取得**する | 以前は `run_for_trigger` の引数で、**analysis 開始前に観測された価格**だった — caller が渡す、最も都合のよい読み。ENTRY の場合だけ、`decision_completed_at` 確定後に observer を呼び、`observed_at >= decision_completed_at` を runner 側で再検査（observer 実装に委ねない）。取得不能は `NO_ENTRY_REFERENCE_PRICE`（attempt は記録され prediction は作らない — 判断が起きて価格が付かなかったことと、判断が起きなかったことは別の事実）。3,000円 filter は `decide()` が entry price で再確認する。**既定 observer は拒否する**（intraday source 未 bind: D-06b / D-103-LIVE）。具体的 method（D-01a）は未決のまま、観測ごとに method を記録するので後から決めても既存記録は無効にならない | 2026-09-18 / 同上 §C | `workers/src/surge/entry/price_observer.py` |
| D-236 | recovery 時の entry price は **recovery 時点で新しく取る。遡及しない** | TX2 後に crash した場合、保存 answer を使い model は再 call しない。`decision_completed_at` が保存済みならそれを保持。entry price が無ければ recovery 時点で取得し、`ENTRY_PRICE_RECOVERY_DELAYED` を evidence に記録する。古い価格を「本来あの時買えた価格」として back-date しない — system がその時点で動いていなかった価格で取引できたと主張することになる | 2026-09-18 / 同上 §C | 同上 |
| D-237 | **watch trigger の production runner は REANALYSIS のみ** | 入口が TRIGGER_HIT なので、ENTRY_DECISION を記録すると「直接 entry した」attempt と「reanalysis」の watch transition が同じ出来事を二通りに記述する。Python（`run_for_trigger`）と DB（`begin_entry_analysis` + CHECK `executions_trigger_path_is_reanalysis`）の両方で拒否。direct-entry path が必要になったら別の入口を作る | 2026-09-18 / 同上 §D | 同上 |
| D-238 | drift 検査の scope は **`pipeline.project_schemas()` と一致させる** | 手で持っていた `PROJECT_SCHEMAS` が `screening` / `material` / `chart` を欠いていた。**4 関数が一度も比較されず、うち `material.stage2_candidates` が drift していた**（他の10件と同じ `--` コメント欠落）。scope が狭い checker は、見えていない部分について「in sync」と報告する — 失敗が静かなのは、狭い scope がエラーではなく短い問題リストを返すから。修正後、cloud の **84 関数すべて** drift 0 / unmanaged 0。integration test が `set(PROJECT_SCHEMAS) == set(pipeline.project_schemas())` を要求する。`storage` は Supabase 管理なので含めない | 2026-09-18 / 同上 §E | `workers/src/surge/runtime/schema_drift.py` |
| D-239 | **Compound の `tool_choice="none"` は必須**（best effort ではない） | D-219 で「Groq は compound に対するこれらを文書化していない」と書いたのは**誤りだった**。Groq API Reference を直接確認: `tool_choice` は "none means the model will not call any tool and instead generates a message"、`disable_tool_validation` は "tool_choice=required/none will still be enforced"、`compound_custom.tools.enabled_tools` は "A list of tool names that are enabled for the request"。ただし**空配列の意味はどこにも書かれていない**（"none" とも「未指定＝既定＝全 tool 有効」とも読める）ので、`enabled_tools=[]` は多層防御として送るが、それだけに依存しない。response 側の `executed_tools` 検査は無条件の第三層として維持。`quota_probe` も同じ2つを送る | 2026-09-18 / 同上 §F | `workers/src/surge/analysis/groq_provider.py` |
| D-240 | **production canonical request は ZDR 確認済みを構造的に要求**する | `privacy_gate_passes` はあったが `_call()` は弱い方（学習に使わない）しか呼んでいなかった。production は Canonical v5.1 全文を毎日何度も送り続ける別の露出であり、**この account で ZDR が ON と確認されている**ことが要る。UNKNOWN は FALSE と同じく拒否（`ANALYSIS_PRIVACY_GATE_BLOCKED`、canonical・addenda・bundle を送らない）。`RESEARCH_SMOKE` は flag ではなく**名前の付いた mode**（固定の無害 prompt のみ、弱い gate）。既定は PRODUCTION | 2026-09-18 / 同上 §G | 同上 |
| D-241 | readiness の analysis provider は **5つの事実に分ける** | provider 名だけで PASS していた。credential configured / quota measured / output mode usable / ZDR confirmed / live smoke passed はそれぞれ**次にやることが違う**。1つにまとめると「Groq を選んだ」と「Groq が動いている」が同じ表示になる。5つとも prediction を gate する。CLI flag も5つに分け、すべて既定 false（まとめた `--analysis-ready` は最も難しいものを最も易しいもので通させる） | 2026-09-18 / 同上 §G | `workers/src/surge/runtime/readiness.py`（`readiness-1.7.0`） |
| D-242 | **verification を attempt → prediction → episode へ伝播**し、DB で prediction = episode を保証する | `entry_db.episode_params()` が `IMPLEMENTED_NOT_LIVE_VERIFIED` を hard-code していた。LIVE_VERIFIED の prediction が未検証の episode の中にある状態を作れた — episode は成績を数える単位なので、同じ主張がどの行を読むかで検証済みにも未検証にもなる。`Episode.verification` を持たせ runner の値を渡す。`check_prediction_matches_attempt` が episode と prediction の verification 不一致を拒否 | 2026-09-18 / 同上 §H | `workers/src/surge/entry/models.py` / `entry/db.py` / migration |
| D-243 | **Input causality: trigger は cutoff より前、bundle の自己申告は実物と照合する** | `begin_entry_analysis` が `trigger.occurred_at <= decision_cutoff_at` を要求する（cutoff が trigger より前なら bundle はそれが反応している出来事より前に組み立てられたことになる）。bundle の宣言も照合する。**prompt hash では足りない**: 新規実行では prompt hash は caller が渡した同じ bundle から計算されるので、**自分自身と一致するのが当然**で何も証明しない。そこで送信前に Python が (1) `sha256(canonical_text) == bundle.canonical_prompt_sha256`、(2) addenda hash を**順序込み**で実テキストと照合、(3) `bundle.decision_cutoff_at == facts.decision_cutoff_at`、(4) decision price の観測時刻 <= cutoff を検査し（`InputInconsistencyError`、TX1 の前なので行は作られない、問題は全件列挙）、DB は Python に分からない半分 — **bundle が watch 自身の security のものか**（watch の security を知っているのは DB だけ）と cutoff・decision price 時刻 — を`20260918020000` で検査する。recovery では canonical/addenda の変化を prompt hash より先に名指しで検出する | 2026-09-18 / ChatGPT 監査 5 §I | `workers/src/surge/analysis/input_snapshot.py` / `supabase/migrations/20260918020000_bundle_agrees_with_its_execution.sql` |
| D-244 | prompt に埋め込む数値は **Decimal の scale に依存しない表記**にする | 再構築検査が実バグを捕まえた: `price_limit_jpy` は `numeric(18,6)` を通ると `Decimal('3000.000000')` で戻り、そのまま埋め込むと元の "3000" と違う文字列になる。つまり**保存入力から再構築した prompt の hash は元と一致し得ず、再開はすべて入力が変わっていないのに `INPUT_RECONSTRUCTION_MISMATCH` で失敗していた**。`canonical_decimal()`（`normalize()` + 固定小数表記）で描画する。既定値の描画は "3000" のまま変わらない | 2026-09-18 / 実装中に発覚（ローカル DB で） | `workers/src/surge/analysis/entry_analysis.py` |
| D-245 | commit するテストは **DB が自ら「使い捨て」と宣言した場合だけ**走る | D-222 の判定は「教師テーブルが 0 件なら使い捨て」だったが、**production も 0-start の規則で 0 件**なので、cloud と使い捨て DB を区別できていなかった。`SURGE_TEST_DATABASE_URL` を取り違えれば、合成 watch・execution・prediction が本番へ commit されていた（後から本物と区別できず、取り消せない）。DB 単位の設定 `surge.disposable_test_database = on` を CI とローカルの test harness だけが付け、fixture はそれが無ければ skip する。**不在は証拠にならないので、不在なら走らない**。教師行チェックは独立した第二条件として残す | 2026-09-18 / 同上 | `workers/tests/test_db_production_entry_analysis.py` / `.github/workflows/ci.yml` |
| D-246 | commit するテストの後始末は **実際に消えるようにする** | append-only guard は owner を含め誰の DELETE も拒否する（それが目的）ので、cleanup は**最初から一度も成功しておらず、例外は握り潰されていた**。前ラウンドで警告に変えたことで初めて見えた。使い捨てと宣言した DB では、その1 transaction だけ `session_replication_role = replica` で trigger を止めて消す（superuser が必要 — test harness はそうで、runtime role は決してそうではない）。episode・state transition・risk line まで消す。ローカル実行で残存 0 件を確認 | 2026-09-18 / 同上 | 同上 |
| D-247 | **credential は DPAPI で暗号化してリポジトリ外に置き、使う1プロセスにだけ渡す** | `Set-SurgeSecret.ps1` が Windows ユーザー単位の DPAPI で `%LOCALAPPDATA%\surge\secrets\<NAME>.dpapi` に保存する。secret は隠しプロンプトか `-FromClipboard` のみ（`-Value` はシェル履歴に残るので secret には拒否）、値の形を検査し、clipboard は finally で消去する。`Invoke-WithSurgeSecrets.ps1` が復号して**そのコマンドの環境変数にだけ**入れ、終了時に消す（呼び出し元シェルへ残っていたのを実測で見つけて直した）。`.env` を作らない・平文をディスクに置かない・値を表示しない。名前は worker が実際に読む5つに限定（typo は保存時に落ちる） | 2026-09-18 / ユーザー指示（credential を会話・ログ・git に出さない） | `ops/windows/Set-SurgeSecret.ps1` / `ops/windows/Invoke-WithSurgeSecrets.ps1` |
| D-248 | **Groq の ZDR は日付つきの観測として持ち、production request の gate にする** | ZDR は console の設定で API からは読めない。`GROQ_ZDR_CONFIRMED_ON=YYYY-MM-DD`（見た日。未来日・不正形式は拒否）を `policy_from_env` が読み、PRODUCTION mode の request は gate を通らなければ送らない（`ANALYSIS_PRIVACY_GATE_BLOCKED`）。canonical を含まない quota probe だけが `RESEARCH_SMOKE` として送れる。2026-09-18 に console で Global ZDR = Enabled（Inference APIs Enabled、Batch・Fine-tuning Off）を再読込後も確認 | 2026-09-18 / 監査 5 §F + console 実見 | `workers/src/surge/analysis/groq_provider.py` |
| ~~D-249~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**Entry price の順序は `decision_completed_at < event_at <= fetched_at`、provenance 必須** | 旧規則は `>=`（同時刻を許可）。同時刻の print は判断の後だと示せず、ns→µs の切り捨てで後の print が同時刻に見えることもあるので拒否する（誤って受理するより誤って拒否する側）。右辺は「取得を終えた時刻より後の print」＝時計の不一致の検出。provenance（provider / feed / exchange / fetched_at / provider の print id / 読み取り回数 / poll window）の無い observation は使わない。保存は既存の `entry_price_evidence text[]` へ `KEY=value` で（schema 変更なし）。**DB の CHECK は `>=` のまま**で backstop。strict 化は migration と cloud 適用を伴うので別途 | 2026-09-18 / ユーザー指示 | `workers/src/surge/entry/price_observer.py` |
| ~~D-250~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**「まだ無い」だけを短い poll window で聞き直す** | 単一取引所では最新 print が判断の直前であることが普通で、即失敗にすると台帳が「モデルが何秒に終わったか」に依存する。`PollingEntryPriceObserver` は既定 window 60 秒・間隔 3 秒、上限 15 分（それ以上は「判断直後」ではなく別の method）。window は**判断時刻から**測る（recovery で遅れて来た呼び出しは1回だけ読む）。聞き直すのは `EntryPriceNotYetObserved` だけで、未知 ticker・credential 拒否・時計の不一致は即失敗。回数と window は provenance に残るので、D-01a で値を変えても過去の記録は誤表示にならない | 2026-09-18 / ユーザー指示（設定可能な短い poll window） | 同上 |
| ~~D-251~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**`AlpacaEntryPriceObserver`（IEX latest trade）は実装し、production には bind しない** | 共通 core `LatestPrintEntryPriceObserver`（market / source / 現在の ticker / method）に Alpaca IEX の source を差した形。`ByMarketEntryPriceObserver` が市場で振り分け、fallback は `FALLBACK_AFTER=n` を残して黙らない。bind しない理由は変わらない: 単一取引所の print を `entry_reference_price` にしてよいかは D-01a / D-103-LIVE。runner の既定は `NoEntryPriceSource`。live 実測（2026-09-18、US 休場中）: 認証・SIP 日足・IEX latest trade/quote は取得できた。判断＝今では 10 秒 poll の後 `EntryPriceNotYetObserved`（正しい）。判断を最新 print の直前に置くと受理され provenance が記録された。24時間取引の crypto feed で同じ core を走らせ、**判断 → 25.5 秒後の print → その 3.4 秒後に取得完了**で順序が live に成立した（crypto は時計の検査専用で entry price ではない） | 2026-09-18 / 実測 | `workers/src/surge/providers/alpaca_iex.py` / `workers/src/surge/jobs/alpaca_smoke.py` |
| ~~D-252~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**`JapanEntryPriceObserver` は形を用意し、source は bind しない** | 同じ core・同じ規則。無料で「価格を1つ保存してよい」JP 場中 source が見つかっていない（D-175 の証券会社 API は蓄積禁止、J-Quants の分足は翌日、CLAUDE.md 1-8）ので、D-06b を名指しして拒否し、attempt は `NO_ENTRY_REFERENCE_PRICE` になる。bind は `source=` を渡すだけ | 2026-09-18 / 同上 | `workers/src/surge/entry/price_observer.py` |
| D-253 | Groq JSON mode の「json という語」規則は **system の framing message** で満たす | `response_format=json_object` の request に "json" の語が無いと Groq は 400。canonical（不変）にも addenda にも書き足さず、schema を示す system message を別に付ける（preflight はその分も数える）。request body は UTF-8（`ensure_ascii=False`）: 以前は `\uXXXX` で 80 KB に膨らんでいた | 2026-09-18 / live の 400 | `workers/src/surge/analysis/groq_provider.py` |
| D-254 | **Groq の HTTP status を failure class に写す** | 413 → `RequestTooLarge`（QUOTA_BLOCKED、terminal）/ 429 → RateLimited（transient）/ 5xx・切断 → ProviderUnavailable（transient）/ 400・422 → RequestRejected（CONTRACT_VIOLATION）/ 401・403・404 → CredentialsRejected（PROVIDER_NOT_CONFIGURED）。以前は HTTPError が OSError として transient に分類され、413 を再試行していた。Groq の error 本文（type / code / message）は残すが org id は `org_<redacted>` に置換 | 2026-09-18 / live | `groq_provider.py` / `analysis/execution.py` / `http_fetch.py` |
| D-255 | compound の probe 記録で **「header の範囲内」を受理の証拠にしない** | compound の rate-limit header は compound system 自身のもの。usage_breakdown に `openai/gpt-oss-120b` と `meta-llama/llama-4-scout` が**prompt 全体を受け取っている**ことが出ており、それらの制限はどの header にも出ない。probe の reason にそう書き、受理は contract smoke だけが決める（readiness は元からそう読んでいる）。preflight も「prompt 単独で超える＝送れない（確実）」と「予約分で超える＝受理され得る（contract smoke が決める）」を分けた | 2026-09-18 / 実測 | `workers/src/surge/jobs/groq_probe.py`（`groq-probe-1.1.0`） / `groq_provider.py` |
| D-256 | **Groq 413 の切り分けは実測だけを記録し、原因を断定しない** | [groq-413-isolation-2026-09-18.md](research/groq-413-isolation-2026-09-18.md)。gpt-oss-120b: 413 本文が「1 request が TPM 8,000 を超える」と明記、**bytes ではなく tokens**（49→299 KB で不変、日本語と ASCII で同 token なら同じ count）、Groq の count は prompt（実 tokenizer）+ completion の見積り（max_completion_tokens ではない）。compound: 境界は o200k 6,368（200）〜6,652（413）、本文はモデルも制限も名指ししない。**「有料化で解消」とは記録しない**（Free/Developer で body size が違うという公式記述は無く、Dev Tier の該当値も未確認）。Entry prompt は 94.7% が不変で、project 側の文言を全部消しても 8,000 を超える | 2026-09-18 / 実測 | research doc |
| ~~D-258~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**JP の entry price source は Yahoo（遅延）: `mea-stock-screener` の provider ロジックを再利用** | ユーザーが個人の投資研究用途として CLAUDE.md 1-1 の**今回限りの例外**を明示許可（2026-09-18）。screener は Yahoo の JSON API（`curl_cffi` の Chrome 偽装 session、`fc.yahoo.com` の cookie、crumb、`v7/finance/quote`、欠けた銘柄は `v8/finance/chart` の meta）を使っており、**web ページの scraping はしていない**。同じ手順を `providers/yahoo_finance.py` に移し `LatestPrintSource` にした。実測: 東京は `quoteSourceName: Delayed Quote`、`exchangeDataDelayedBy: 20`、最新約定は約 15 分前、`regularMarketTime` は秒精度の最終約定時刻。遅延 feed でも**判断後の約定を遅れて観測する**ことはできるので、poll が遅延を待ち切ってから最新約定を採る。順序 `decision < event <= fetched` は real-time feed と同じに成り立つ（遅延は `FEED_DELAY_SECONDS` として価格と一緒に保存）。非公開 API なので method・provenance に `YAHOO_UNOFFICIAL_JSON_API` / `PERSONAL_RESEARCH_USE` を残す。`decision_price` には使わない（D-21 はその部分が未決のまま） | 2026-09-18 / ユーザー指示 + 実測 | `workers/src/surge/providers/yahoo_finance.py` / `workers/src/surge/entry/observers.py` |
| ~~D-259~~ | （**D-261 で superseded**。場中の判断後約定を待つ設計の記録で、該当コードは 2026-09-18 に削除）**遅延 feed の poll は遅延を待ち切り、その間を叩き続けない** | `PollingEntryPriceObserver.feed_delay`（上限 30 分）: 締切 = 判断 + 遅延 + window。source が「最新約定が判断よりどれだけ前か」（`behind_by`）を返すので、その分だけ眠る（interval 未満にはしない・締切は越えない）。20 分の遅延でも読み取りは 2〜3 回。最後の読み取りは締切ちょうどに行う。休場中の real-time feed でも同じ理由で 20 回ではなく 2 回で済む。JP の既定は遅延 20 分 + window 5 分・間隔 15 秒、US は window 60 秒・間隔 3 秒のまま | 2026-09-18 / 実装 | `workers/src/surge/entry/price_observer.py` / `workers/src/surge/entry/observers.py` |
| D-261 | **前提修正: Prediction は市場終了後に実行し、基準価格は対象セッションの確定終値** | ユーザー指示（2026-09-18）。Prediction / analysis は引け後に走り、基準価格は対象セッションの**確定終値**＝ `signal_reference_price`（v5.1 の「分析基準価格」。v5.1 は「前営業日終値を分析基準価格とする場合」を扱い、20% Threshold を「分析基準価格 × 1.20」とする）。この価格は `decision_completed_at` より**前**の market event でよく、必須なのは「対象営業日の市場終了後に取得された、その日の確定終値であること」（D-262）。**整合性**: v5.1 とは一致。衝突するのは post-v5.1 の addenda（Phase 0.1 #6 / Phase 0.2 #3 / 最終パッチ #4: EOD は Setup のみ・Prediction は場中 ENTRY のみ・成績は判断後の `entry_reference_price`）と、それを写した CLAUDE.md 1-4・lifecycle spec・DB CHECK。addenda は glob で**全 LLM prompt に入る**ので、LLM 向けの新 addendum は EOD Prediction の実装時に文言を確定して追加する（今足すと場中 Entry prompt と矛盾する指示が入る）。将来の実売買検証（`execution_price` / `next_session_entry_price`、翌営業日の寄り付き後など）は Prediction とは別の観測で、今回は実装しない。realtime polling は Prediction 生成に使わない | 2026-09-18 / ユーザー指示 | CLAUDE.md 1-4 の注記 / `docs/specs/entry-and-episode-lifecycle.md` の注記 |
| D-262 | **確定終値の価格源: JP = Yahoo 日足、US = Alpaca SIP 日足** | JP: Yahoo の chart endpoint の日足（raw close）。取得ロジックはユーザーの screener（`mea-stock-screener`）のもの（CLAUDE.md 1-1 の今回限りの例外、個人研究用途）。東京は約 20 分遅延と宣言されるので、**セッション終了（15:30 JST、2024-11-05 より前は 15:00）+ 20 分以降に読んだ値だけ**を確定とする。当日足は Yahoo 上で終値の約定時刻がセッション終了以降になっていることも要求し、終値で約定しない薄い銘柄は遅延の 2 倍（40 分）経過で最終約定を終値とみなす。US: Alpaca SIP の日足（`feed=sip`、raw）。無料プランの 15 分遅延 + 1 分余裕の後、セッション終了（16:00 ET = 日足 stamp + 16 時間。短縮取引日は遅い側に倒れるだけ）より後に読んだものだけを確定とする。どちらも provider / feed / basis / session 終了時刻 / 取得時刻 / 遅延を記録する。2026-09-18 実測: JP 9/17 の 7203・9432 は確定、JP 9/18（場中）は正しく「未確定」、US 9/17 の AAPL・SPY は確定。Yahoo は非公式 API であることを許容（個人研究） | 2026-09-18 / ユーザー指示 + 実測 | `workers/src/surge/entry/session_close.py` / `workers/src/surge/providers/yahoo_finance.py` / `alpaca_historical.session_close` |
| D-263 | **`entry_reference_price` は改名しない。意味だけ整理する** | 名称は addenda（Phase 0.1 #6 / 0.2 #3 / 最終パッチ #4）・CLAUDE.md 1-4・DB 列（`prod.predictions` / `prod.entry_attempts` / `prod.entry_analysis_executions`）・regression fixtures で固定されている。D-261 の下では、Prediction の基準価格は `signal_reference_price`（確定終値）で、`entry_reference_price` は将来の実売買検証の価格（`execution_price` / `next_session_entry_price`）に対応する既存の列として残す。改名 migration は作らない | 2026-09-18 / ユーザー指示（Canonical で固定なら改名しない） | 同上 |
| D-264 | **DB の `entry_price_observed_at >= decision_completed_at` を `>` にする migration は作らない** | その制約は場中 ENTRY の判断後価格のためのもので、D-261 で Prediction 生成に使わなくなった。今 `>` にしても意味が無く、EOD Prediction を実装するときには確定終値（判断より前）をどう保存するかと一緒に制約そのものを見直す（D-265） | 2026-09-18 / D-261 の帰結 | — |
| D-266 | **無料 Groq はここで打ち切る: 直接の llama-4-scout はこのアカウントでは使えない** | 最後の候補だった `meta-llama/llama-4-scout-17b-16e-instruct` の直接利用を、Entry request を1回だけ送る形（`groq_probe --contract-smoke --single-attempt`: schema 再試行なし・transport 再試行なし）で試した。事前の小さな quota probe が **HTTP 404 `model_not_found`（"does not exist or you do not have access to it"）** で、Entry request は送られていない（token は使っていない）。`/v1/models` の一覧（metadata のみ）でも、このアカウントが直接呼べる chat モデルは `groq/compound` / `groq/compound-mini` / `openai/gpt-oss-120b` / `openai/gpt-oss-20b` / `openai/gpt-oss-safeguard-20b` / `qwen/qwen3.8-27b` / `allam-2-7b` だけ（llama-4-scout は compound の内部でのみ使われている）。汎用の gpt-oss-120b / gpt-oss-20b / qwen3.8-27b の TPM はいずれも 8K（console の limits ページで確認、gpt-oss-120b は 413 本文でも実測、D-256）で、約 14K tokens の Entry request は送れない（safeguard-20b は安全分類用で対象外、TPM は未確認）。compound 系も 413。**ユーザー指示どおり、無料 Groq にはこれ以上時間を使わず、別の無料モデル / provider を検討する**（D-11） | 2026-09-18 / 実測 + ユーザー指示 | `workers/src/surge/jobs/groq_probe.py`（`--single-attempt`） |
| D-267 | **EOD Prediction の規則（eod-prediction-1.0.0）** | ユーザー決定（2026-09-18、[原文](requirements/user-decision-2026-09-18-eod-prediction-rules.original.txt)）: S0 = 予測対象日の確定終値 / Prediction は S0 の終値確定後に実行 / 3,000 円 Hard Filter は S0 確定終値で判定 / 目標価格 = S0 確定終値 × 1.20 / 評価開始 = 翌営業日 S1 / outcome checkpoint = T+1・T+3・T+5・T+10・T+20 / 最終評価期限 = T+20 営業日。`success_label` と +20% 到達（`hit_20`、指示文では `hit_20_percent`）は**同義にしない**。反映: 仕様 [eod-prediction.md](specs/eod-prediction.md)、LLM 向け [addendum](prompts/addenda/v5.1-addendum-2026-09-18-eod-prediction.md)（MANIFEST 登録、原文2本も登録）、DB は**新しい append-only table `prod.eod_predictions`**（場中の `prod.predictions` の制約は緩めない）で、確定の条件・時刻順序・3,000 円・目標価格・S1 開始・checkpoint・T+20・mock 禁止・INCLUDED のみ・1 セッション1件を CHECK / UNIQUE で強制。Python は `surge.entry.eod_prediction`。v5.1 の分析基準価格とは一致。`initial_failure_line` は与えられたときだけ検査（EOD で必須にするかは Outcome 側の規則として後で決める。NULL 許容は後から NOT NULL にできる） | 2026-09-18 / ユーザー決定 | `supabase/migrations/20260918030000_eod_predictions.sql` / `workers/src/surge/entry/eod_prediction.py` |
| D-269 | **Jev（TypeSafe）適合性 smoke: Vercel AI Gateway 経由で成立。技術的適合は成功扱い、production default にはしない** | **ユーザー判断（2026-09-18）: API・schema・速度・費用の適合は確認済みとして成功扱い。株価 Prediction としての予測品質は未検証なので、production default provider は変えない**（次は過去データでの予測性能検証）。Node runner は `ops/jev-gateway-runner/`（`ai` 7.0.106 固定）。TypeSafe 直接 API 用の `send` は将来の比較用に残すが、`jev_questions` / `jev_smoke` は production 経路のどこからも import されていない。以下は smoke の事実: | TypeSafe 直接 API は early access のため、ユーザー指示で Vercel AI Gateway（`typesafe-ai/jev`、AI SDK `experimental_evaluate`。evaluation は AI SDK 専用で OpenAI 互換 endpoint では不可）経由で実施。Hobby team、無料 credit $5.00、Auto-reload Off、有料 credit 購入・カード操作なし。key は DPAPI ストア（`AI_GATEWAY_API_KEY`）。**質問の対応**（`surge.analysis.jev_questions`）: `state`→choice、zone basis 6種→noul×6、reachable zone→5段の score、価格・目標・3,000円・日付はコードが決め Jev に聞かない、文章項目は落とす。Gateway との差は `noul`↔`boolean`（答えは `probability`）と、choice/score の confidence が `providerMetadata.typesafe.confidence` 側にあることだけで、adapter で吸収。**実測**（実データ1銘柄、S0 2026-09-17、Canonical と addenda は全文）: 入力 21,858 tokens（o200k 17,750 の約1.23倍）、1回 $0.000918（$0.042/M、出力は無料）、end-to-end 0.92–1.02 秒（provider 0.25–0.38 秒）、9問すべて回答・型一致。同一入力3回で decision は3回とも同じ、choice の確率は最大 0.17 動き（confidence 0.69→0.48）、noul は ±0.02 以内、score は最大 0.17。事前の極小リクエスト（非 Canonical）1回を含め計4回、Gateway cost 合計 $0.00277 | 2026-09-18 / 実測 | `workers/src/surge/analysis/jev_questions.py` / `workers/src/surge/jobs/jev_smoke.py` |
| D-270 | **Jev 予測性能評価の設計（jev-eval-1.0.0）** | **ユーザー決定（2026-09-18）**: retrospective と prospective を分離する。**Phase A**（S0 2025-01-01〜2026-06-30 から point-in-time に 100 件）は state 再構築・screening replay・Jev request・outcome 計算・report・schema / hash / provenance・API cost accounting の検証**だけ**で、成績は採用判断に使わない（Jev が outcome を学習で見ている可能性を排除できない）。**Phase B**（S0 2026-09-21 以降に新しく発生する候補の shadow prediction、1,500〜2,000 件、T+20 まで追跡）で正式に判断する。Phase A が正常に終わるまで Phase B の大量実行はしない。母集団: Primary（point-in-time screener 通過）: Control（同日の 3,000 円以下 Universe で非通過、matched sampling）= 75:25、**両群を混ぜて calibration を計算しない**（Primary: Brier / Brier skill・calibration・PR-AUC・hit rate・state 別実績・score と将来リターン。Control は pipeline 全体の追加価値を見る benchmark 専用）。第1版は JP のみ。匿名化 sensitivity を約 10%（同じ state の匿名版を1回、ticker・企業名・issuer 名・直接特定する文字列だけを伏せ、main に混ぜない）、drift を約 5%（本番を3回平均にはしない）。保存は DB ではなく immutable files（`<評価ルート>/evaluation/jev/<run_id>/`、manifest ほか）、全行 `teacher_admissible = false`、teacher / prediction / execution 等の production DB へは書かない。Outcome は D-268 が未決なので +20% 到達を高値・終値の両方で計算し、T+1/3/5/10/20・max upside・max drawdown を固定して保存、`success_label` とは統合しない。API 予算: Phase A 100 件まで（+ 匿名化・drift）、Phase B 1,500〜2,000 件、run ごとの hard budget、Vercel AI Gateway の無料 $5 credit を超えない（有料 credit・auto-reload なし）。**Claude Code が決めた実装上の事項**: addenda は MANIFEST の登録順で送る（ファイル名順だと `v5.1-addendum-2026-09-15.md` が、後から登録されそれを改める phase0.x の後ろに来る）/ S0 は月 2 日（seed 付き）、同一銘柄の S0 は 20 営業日以上離す / Control は同じ S0 で価格帯 → 流動性帯（その日の適格銘柄の 20 日平均売買代金の四分位）の順に近いもの / 開示タイトルは cutoff（S0 の夜）の 30 分以上前に公表されたものだけ / 見積もりは o200k × 1.35（実測比 1.23）× $0.042/M、支出は Gateway の報告額、報告の無い request は見積もり額で計上。Phase A の制約（Routes A–H のみの replay、現在の JPX 一覧による生存者バイアス、seed 付き部分集合、売買代金の近似、開示 index の到達範囲）は設計書 9-4 | 2026-09-18 / ユーザー決定 | `docs/specs/jev-evaluation-design.md` / `workers/src/surge/evaluation/` / `workers/src/surge/jobs/jev_eval.py` |
| D-271 | **Yahoo の日足は後の分割で遡って調整されている: as-traded に戻して使う** | 実測（2026-09-18）: v8 chart の日足の OHLC と出来高は、その後の分割で遡って調整されている（Sony 6758.T 5:1・SMFG 8316.T 3:1、いずれも Yahoo の ex date 2024-09-27）。`events=split` の ex date と比率から as-traded に戻す（価格 × その日より後の分割倍率の積、0.1 円に丸める。出来高は ÷）。**D-262 の JP 確定終値（`session_close`）も同じ修正**: S0 の後に分割があると、読み直した終値が調整済みの値になり、3,000 円判定と目標価格を誤る（当日夜の通常の読み取りでは起きにくいが、再読み取り・replay で起きる）。評価（D-270）は as-traded の bar と分割の `CanonicalAction` から `build_comparable_series(as_of=S0)` で S0 時点の株数に換算し、Outcome は S0 の株数基準に換算する（CLAUDE.md 1-9） | 2026-09-18 / 実測 | `workers/src/surge/providers/yahoo_finance.py` / `workers/src/surge/evaluation/prices.py` |
| D-273 | **Gateway free tier の rate limit: Jev への送信は 15 秒間隔・60 秒に最大 4 件、429 は即停止** | **事実**: Phase A の正式 run `jev-a-20260918-03`（commit 428ec1c、preflight 違反 0）で 2026-09-18 09:28:34〜45 UTC に約 2.2 秒間隔で送り、5 件成功（入力 22,382〜22,785 tokens、計 $0.00472773）、**6 件目が HTTP 429 `GatewayRateLimitError`**（`rate_limit_exceeded`、「Free tier requests on this model are rate-limited」）。停止条件どおり停止し再試行なし、429 は無課金（残高差 = 5 件の cost 合計）。窓の大きさは Vercel の docs に無く、429 の本文にも retry-after は無かった（当時の runner は応答 header を記録していなかった）。**窓の確認（ユーザー指示）**: 最後の 429 から 44.9 分後の 10:13:43 UTC に非 Canonical の極小 request を 1 件だけ送り **200**（323 tokens、$0.000013566）→ 日次上限ではなく短い窓の rate limit として扱う。**ユーザー決定（2026-09-18）**: 「5 requests/minute」とは断定しないが、実測（5 件成功 → 6 件目 429）を踏まえ、評価 run は **最短 15 秒間隔かつ任意の 60 秒に最大 4 件**（rolling window の guard、`surge.evaluation.pacing`）。429 は 1 件でも即停止・再試行なし・有料 credit 購入なし・Auto-reload 変更なし・hard budget $0.20 を維持。429 では HTTP status・error type・応答 header・Retry-After（無ければ null）・request 時刻・直前の成功時刻・rolling 60 秒の件数を記録する（API key と request 本文は記録しない）。停止した `-03` は監査記録として変更・再開せず、新しい run `-04` で 115 件を最初から送る（`-03` の 5 件は再利用しない、重複 約 $0.005 は許容）。60 秒後の probe も 429 だった場合は、その日は Phase A を続行しない、という条件だった | 2026-09-18 / 実測 + ユーザー決定 | `workers/src/surge/evaluation/pacing.py` / `workers/src/surge/jobs/jev_eval.py` / `ops/jev-gateway-runner/describe-error.mjs` |
| D-274 | **Phase A は throughput 調査を打ち切り、5 分間隔・24 件の縮小 run で pipeline 検証を優先する** | **事実**: Vercel AI Gateway の free tier では、**5 件連続の後の 6 件目で 429 を 2 回再現**した（`jev-a-20260918-03`: 約 2.2 秒間隔、`jev-a-20260918-04`: 15 秒間隔かつ 60 秒に最大 4 件の pacing、commit 18e1da1）。**15 秒 pacing / rolling 60 秒 4 件でも解消しなかった**（`-04` の 6 件目 10:32:17 UTC、直前 60 秒の件数 4）。正確な window / token quota は**公開情報からも response header からも特定できない**（`-04` から記録した 429 の header に Retry-After・rate-limit 系 header は無い）。観測上は約 22.5K tokens の request がある窓の中で 5 件まで、窓は 75 秒超・18.3 分未満。各 run の実費は 5 件分 $0.00472773、429 は無課金。**ユーザー決定（2026-09-18）**: Phase A の目的はモデル性能評価ではなく evaluation pipeline の end-to-end 検証なので、**規模を main 20（Primary 15 / Control 5）+ 匿名化 2 + drift 2 = 24 request に縮小**し、**5 分間隔**で送る（hard budget $0.05、429 は 1 回で即停止・再試行なし・有料 credit なし・Auto-reload 変更なし）。sample は正式な母集団（75 / 25、D-270）から固定 seed で決定論的に縮小し、outcome を見て選ばない（`reduce_population`: cohort と sample id だけを読む。匿名化・drift も固定 seed で main から、互いに重ならないよう選ぶ）。24 件で request・schema・保存・Primary / Control 分離・outcome join・確率 / score 集計・匿名化比較・drift 比較・report・実 token / cost・provenance・リーク検査がすべて動けば Phase A 成功。精度・Brier・PR-AUC の値は採用判断に使わず、少数で未定義になる統計は null のまま扱う。**rate limit の境界探索はこれ以上しない。** **Phase B で Vercel Free 経由の大量 shadow を前提にしない**（Phase A 完了後に、TypeSafe 直接 API access・Gateway 以外の Jev 提供経路・日をまたぐ低速 batch・別 provider との比較を検討する。Phase B の設計はまだ変えない）。`-03` / `-04` は停止済みの監査記録として残す。付記（評価には使わない）: `-03` と `-04` で同じ 5 request が約 1 時間の間隔で答えられ、decision が 5 件中 2 件で変わった（いずれも選択肢の確率が近接） | 2026-09-18 / 実測 + ユーザー決定 | `workers/src/surge/evaluation/population.py`（`reduce_population`）/ `workers/src/surge/evaluation/pacing.py` / `workers/src/surge/jobs/jev_eval.py` |
| D-275 | **Phase B の主経路は TypeSafe 直接 API、Vercel AI Gateway は fallback** | **事実（2026-09-19）**: TypeSafe console（ユーザー本人がサインイン）では **waitlist 不要で direct API key を即時発行**できた。**`jev-latest` の direct API は正常に利用できる**（`POST https://api.typesafe.ai/v1/systemone`、応答の `model` は版名 `jev-1.13.0`）。Vercel Gateway と**同一 state / questions の 1 件比較に成功**（Phase A `-05` の `341b8bfcd53b6a70.main`。違いは API ごとの wire 形式 = model 名とyes/no 型名 `noul` だけ）: direct は **22,605 input tokens / 211 output tokens / latency 約 1.03 秒 / cost 約 $0.000949**（公表単価 $0.042/M × input tokens。TypeSafe は cost を返さない）/ **schema complete**（confidence は choice と score に同梱）。**出力は Gateway で観測した run-to-run variance の範囲内**（同じ request の Gateway 3 回と同じ REJECT、REJECT 確率 0.43 は Gateway の 0.38〜0.52 の中、reaches_target 0.17 は 0.18〜0.19 とほぼ同じ）。TypeSafe account には**月次 $5 の promotional credit**（2026-09-19 付与、10-19 失効）。**有料契約・カード登録・auto recharge なし**。Phase B の 1,500〜2,000 件は、現在の実測単価（約 $0.00095 / 件）なら**約 $1.4〜$1.9 で無料 credit 内に収まる**。**throughput smoke**（同じ評価用 request、2 秒間隔、最大 6 件）: **6 / 6 成功**（HTTP 200、latency 0.89〜1.04 秒、各 22,605 tokens、計 $0.0057）、rate-limit 系 header と Retry-After は無し → Gateway で再現した「5 件成功 → 6 件目 429」は direct では起きない。throughput の上限探索はここで終える。**ユーザー決定**: Phase B の主経路 = TypeSafe direct（model `jev-latest`、初期 pacing 1 request / 5 秒）、**Vercel Gateway は rate limit のため fallback**（direct の障害時、direct / Gateway の出力比較、adapter の regression test。Phase B の通常経路には使わない、削除しない）。有料 credit は購入しない。**Jev を production prediction に接続する判断はまだしない**（Phase B は shadow evaluation）。direct access の確認 1 件と throughput smoke 6 件は評価 dataset に混ぜない（`teacher_admissible = false`、`~/.surge/evaluation/jev/direct-*` に別置き）。**adapter（Claude Code 決定）**: `surge.evaluation.providers`（TypeSafe direct / Gateway）。送信前に monthly credit を確認する: TypeSafe に残高 API が無い（公式 SDK・API reference に無い）ので、operator が console の Credit Balance を読んだ値と時刻（7 日以内）から、この system がその後に記録した direct の支出を引いた額で判定。run ごとの hard monetary budget。429 は provider の `retry_after_ms`（`retry-after-ms` header → `Retry-After` → body）を優先して待って同じ request を再送（最大 3 回、1 回の待ちは 30 分まで）、値が無ければ 60 秒から倍々の backoff、超えたら停止。served version（`jev-1.13.0`）を run の最初の回答で固定し、run 中に変われば停止。raw の Jev 出力（加工前）を保存。request 本文・Canonical・API key は記録・出力しない | 2026-09-19 / 実測 + ユーザー決定 | `workers/src/surge/evaluation/providers.py` / `workers/src/surge/evaluation/pacing.py` / `workers/src/surge/evaluation/cost.py` / `workers/src/surge/jobs/jev_eval.py` |
| D-276 | **Jev 評価 Phase B の規則（D-272 の解決）: 1 営業日最大 92 request、決定論的な一様抽出、Route D は変えず subgroup で別集計、S0 = 2026-09-24 から、`jev-1.13.0` に固定、Phase B 全体の hard cap $2.50** | **ユーザー決定（2026-09-19）**: (1) 1 営業日の main shadow prediction は最大 80（Primary 60 + Control 20）、匿名化 = main の 10%（8）、drift = 5%（4）→ 最大 92 request / 営業日、TypeSafe direct 1 request / 5 秒、目標 25 営業日（Primary 1,500 / Control 500、約 2,300 request ≈ $2.2）、有料 credit は買わない。(2) Routes A–H は Phase B 中変えない。営業日ごとにその日の 3,000 円以下 universe へ現行 screener を point-in-time で適用し、通過・非通過の全銘柄と各 route の membership を保存してから最大 80 件を抽出。(3) Primary 60 は通過銘柄から outcome も Jev の出力も見ずに決定論的な一様抽出（seed に evaluation version・trading date・固定 experiment seed）、同じ入力なら同じ 60、60 以下なら全件、未選択も candidate pool に残し selected・selection probability・route membership・D-only を記録。(4) Route D の閾値・定義は変えず、D-only を除外・減量しない。事前登録の subgroup（D-only / D+other / non-D）で reaches_target の calibration・+20% 到達率・decision 分布・score・将来の最大上昇・最大 drawdown を別集計、変更の検討は Phase B の後。(5) Control 20 は同日の非通過銘柄から価格帯・流動性帯で match、可能なら sector も（sample を大きく失わない範囲）、同点は決定論的 hash、outcome は使わない。(6) S0 = 2026-09-24 から（9/21〜23 は休場）、それより前は拒否。(7) TypeSafe direct、served version を保存、公式に可能なら `jev-1.13.0` に pin（不可なら `jev-latest` + 版の変化で自動停止）、別の版を黙って混ぜない。(8) credit は console snapshot（confirmed balance・confirmed_at・表示された expiry）+ local の支出 ledger、任意の 7 日再確認は設けない（D-275 の 7 日規則を廃止）、expiry を跨ぐときだけ停止して新しい残高を一度確認、run ごとの hard budget は維持、**Phase B 全体の TypeSafe direct は $2.50 が hard cap**。(9) Phase B 中は Canonical・addenda・question schema・route 定義・sampling algorithm・outcome 定義・decision の解釈を固定。(10) probability / confidence の閾値・3 回平均・コード側の decision override・Jev の production 利用・teacher dataset への登録は導入しない。Raw output をそのまま評価。**確認（2026-09-19）**: TypeSafe の Models 文書は版 ID （例 `jev-1.13.0`）を `model` に受け付ける。実測 1 件（非 Canonical・銘柄なしの極小 state、評価 dataset ではない）で `model: "jev-1.13.0"` → HTTP 200、応答の `model` = `jev-1.13.0`（338 tokens、$0.0000142）→ **Phase B は `jev-1.13.0` に pin**、違う版の回答で日と cohort を停止、Gateway（回答に版が無い）は cohort に入れない。credit snapshot を記録: 2026-09-19 08:43:13Z の console（Credit Balance $5.00、Monthly credit、Expires (UTC) Oct 19, 2026）、その後の local 支出 $0.006660 → 残 $4.993340。**Claude Code の決定（可逆・安全側）**: key = SHA-256（version|seed|S0|目的|code）の小さい順（bottom-k）、Primary が 60 未満の日は Control を 3 : 1（切り上げ）、Control の match は 価格帯+流動性帯+sector → 価格帯+流動性帯 → 価格帯 → 流動性帯 → その他（同段は hash 順）、匿名化 / drift は main の 10% / 5%（切り捨て）、送信窓 = S0 16:10 JST（遅延 20 分 × 2、D-262）〜翌平日 09:00 JST（各 request の直前に確認）、universe の読み込みが 95% 未満の日は抽出しない、履歴は S0 − 400 日から、表示された失効日は 00:00 UTC とみなす、版の違う回答のみ cohort を停止（他は日だけ停止・再開しない・部分的な日は集計しない）、1 日の budget $0.12 / 92 件、全件送信済みの日が 25 に達したら新しい日を plan しない、1 銘柄の取得失敗はその銘柄だけの失敗として記録、凍結対象には state.py・universe.py も含める、Phase B の outcome は T+20 確定後に日足を読み直して計算。**実データでの予行**（S0 = 2026-09-18、cohort なし・送信なし）: 内国普通株 3,700 の履歴を全件読めた（失敗 0、28.4 分）、適格 2,867、通過 2,066（72.1%、D-only 713）、抽出 Primary 60（D-only 23）/ Control 20（18 件は sector まで一致）/ 8 / 4 = 92。**状態: 実装と synthetic test（Phase B 19 件、評価全体 75 件）まで。cohort 未作成・送信なし・OS の scheduler 未登録**。詳細は [jev-evaluation-design.md](specs/jev-evaluation-design.md) §11 | 2026-09-19 / ユーザー決定 + 実測 | `workers/src/surge/evaluation/selection.py` / `phase_b.py` / `cost.py` / `providers.py` / `report.py` / `workers/src/surge/jobs/jev_eval.py` / `workers/tests/test_evaluation_phase_b.py` |
| D-277 | **Jev 評価 Phase B の開始条件と自動運用: cohort `jev-phase-b-jp-20260924-v1`、Windows Task Scheduler （平日 16:10 JST）、営業日は job 自身の JPX calendar** | **ユーザー決定（2026-09-19）**: cohort_id `jev-phase-b-jp-20260924-v1`、experiment_seed `surge-phase-b-jp-jev-1.13.0-v1-20260924`（Phase B 終了まで変えない）。自動運用（Task Scheduler、平日 16:10 JST、手動承認なし）: universe 取得 → Yahoo EOD → screening → candidate pool 保存 → Primary / Control 抽出 → 匿名化 / drift → build → prospective preflight → TypeSafe direct 送信 → artifacts / ledger 保存を一連で。営業日の判定は job 自身の JPX calendar を authority とし、土日・祝日・非取引日は何も送らず正常終了。自動停止: `jev-1.13.0` 以外 / 凍結 hash の不一致 / Canonical・addenda・question schema の変更 / coverage < 95% / 開始日前 / 送信窓の外 / daily budget 超過 / $2.50 cap 超過 / credit snapshot 失効 / 回答 schema 不完全 / provider error / leakage・integrity violation / 25 営業日完了。429 は既存仕様どおり。credit は 2026-10-19 の失効で自動停止を維持し、新しい credit の後も**ユーザーが console 残高を確認するまで再開しない**。scheduler は実行コマンド・working directory・clean / frozen worktree・実行ユーザー・DPAPI・ログ・二重起動防止を確認し、同じ営業日の二重起動で二重送信しない lock / idempotency を必須、スリープ・再起動で遅れた起動は送信窓の中なら実行・外なら送らず終了。2026-09-24 16:10 JST より前は送らない。production 接続・teacher 登録・route 変更・閾値・複数回平均・decision の上書きはしない。**Claude Code の決定（可逆・安全側）**: calendar = JPX「営業時間・休業日一覧」（2026/02/06 更新、2026-09-19 読み取り、ページの SHA-256 を記録）の 2026・2027 年の休業日 + 土日、範囲外の年は答えない（推測しない）。job がいつ動くかだけを決め、outcome の窓は D-142 のまま（実際に立った session）。calendar 上の営業日にデータの session が無ければ異常としてその日を止める。lock = cohort ごとの OS の byte-range lock（process が終われば OS が外す。乗っ取らない）+ Task Scheduler の IgnoreNew。冪等性: `stage-run` の日は送らない、`run-stopped` / `day-stopped` の日は自動で再試行しない、`run-started` だけが残った日は再開しない（記録の無い送信があり得る）。guard が止めた日は `day-stopped` を残し exit 2、何もしない日は exit 0、エラーは 1、wrapper が起動できなければ 3。早い起動は 15 分まで窓を待つ。frozen worktree = 登録 commit の detached git worktree で、job は別の場所・commit・変更ありでは動かない。wrapper は `TYPESAFE_API_KEY` だけを復号。**実測（2026-09-19）**: Claude desktop app の中から Set-SurgeSecret.ps1 で保存した key は MSIX の仮想化で app の package cache（`%LOCALAPPDATA%\Packages\Claude_*\LocalCache\Local\surge\secrets`）に実体があり、app の外で動く Task Scheduler からは標準の場所に見えない（初回の手動起動が exit 3 で送信前に止まって判明）。**状態（2026-09-19）**: cohort 作成済み、prediction task を frozen worktree で登録済み（D-278 後は commit `3289fdf`）、最初に送信し得るのは 9/24 16:10 JST。手動起動で `closed_day` / `locked`（いずれも exit 0、送信なし）を確認。Python と依存 package・`~/.surge`・worktree は app の外からも同じに見える（一時 task で確認）→ wrapper は標準の場所、無ければ package cache の 1 件を使い、使った path をログに残す。preflight は key が環境に無ければ ready にしない。task は現在のユーザー・ログオン中のみ・通常権限、StartWhenAvailable、IgnoreNew、battery でも実行・停止しない、3 時間の上限、再起動なし。詳細は [jev-evaluation-design.md](specs/jev-evaluation-design.md) §11-7 | 2026-09-19 / ユーザー決定 | `workers/src/surge/evaluation/jpx_calendar.py` / `joblock.py` / `phase_b.py` / `workers/src/surge/jobs/jev_eval.py`（`phase-b-scheduled`・`check-calendar`）/ `ops/windows/Invoke-JevPhaseB.ps1` / `ops/windows/Register-JevPhaseBTask.ps1` |
| D-278 | **Jev 評価 Phase B の outcome と report を自動化（`phase-b-outcome-scheduled`、平日 18:00 JST）、両 task に WakeToRun** | **ユーザー決定（2026-09-19）**: prediction 側は完成扱い、9/24 まで Canonical・addenda・Routes・sampling・question schema・model version・selection logic を変えない。T+20 の outcome 確定・rolling report・最終 report を自動化し、production DB / teacher DB には書かず Phase B の evaluation artifact だけを更新する。outcome job は別の scheduled entry point で営業日 18:00 JST、対象は outcome 未確定・T+20 営業日が終了済み・Yahoo EOD が確定済みの S0 だけ、計算は凍結済みの定義そのまま（T+1/3/5/10/20、+20% の高値・終値到達、max upside、max drawdown、`success_label` と統合しない）、write-once、同じ日を何度実行しても二重に作らない。途中の report は `status = partial`・resolved / unresolved の件数・cohort completion 率を示し、未解決を失敗扱い・母数に混ぜない。25 営業日の収集後も最後の T+20 が確定するまでは final にせず、全 outcome が確定した時点でのみ `status = final`。prediction と同じ frozen worktree・cohort protocol、同じ cohort の OS lock で排他。closed day / 対象なし → no-op、frozen hash 不一致・coverage 不足・integrity violation → stop、outcome job から Jev を呼ばない。両 task に WakeToRun、「ログオン中のみ」と StartWhenAvailable は維持（資格情報は保存しない）。credit 失効でのprediction 停止は維持（ここだけ人の操作: console で残高を 1 回確認して snapshot を更新してから再開）、outcome job は止めない。**Claude Code の決定（可逆・安全側）**: 期限の判定は JPX calendar の T+20（`business_day_after`）とその 16:10 JST、窓の中身はデータ上の session（D-142）で、データに session が足りなければ「まだ」として何もしない。確定は `outcome-attempts/<n>/` に全部を計算してから書き、最後の `stage-outcomes.json` が commit 点（落ちても半端な outcome は残らない）。coverage 不足の S0 は確定せず exit 2、次の営業日に自動で再試行（prediction と違い時間で失われない）。outcome が 1 件も無い間は report を書かない（no-op）。rolling report は確定済み outcome の集合が変わったときだけ、final は `report-final.json` を 1 度だけ。毎回の実行で送信済み全日の `stage-run` と確定済み `stage-outcomes` を照合。cohort completion 率 = outcome 確定済みの営業日 / 25。unresolved は T+20 待ちとデータ欠損（UNRESOLVED_MISSING_DATA）を分けて数える。wrapper は outcome job では key を復号しない。**状態（2026-09-19）**: frozen worktree を commit `3289fdf` に移し（cohort の fingerprint は同一）、prediction task を WakeToRun 付きで再登録、outcome task（平日 18:00）を登録。どちらも土曜は `closed_day`、lock 保持中は `locked`（exit 0）を実機で確認。wake timer は AC で許可・battery で不許可（未変更）。詳細は [jev-evaluation-design.md](specs/jev-evaluation-design.md) §11-9 | 2026-09-19 / ユーザー決定 | `workers/src/surge/jobs/jev_eval.py`（`phase-b-outcome-scheduled`・`freeze-outcomes`・`phase-b-report`）/ `workers/src/surge/evaluation/report.py` / `jpx_calendar.py` / `ops/windows/Invoke-JevPhaseB.ps1` / `ops/windows/Register-JevPhaseBTask.ps1` |
| D-279 | **Phase B の Web shadow（Vercel）: 正式系は PC のまま、クラウドに並走の shadow を作る** | **ユーザー決定（2026-09-19）**: SURGE を Vercel 上で完結する Web アプリにする。ただし凍結済みの Phase B cohort と Windows Scheduler は変更・削除せず、Web 版はまず parallel shadow として作る。既存の `apps/web` を拡張し（別 project を乱立させない）、画面は `/`・`/predictions`・`/runs`・`/outcomes`・`/reports`・`/system`（API key は表示しない）。処理は Vercel Workflows（可能なら Python Workflow SDK）で step に分け、Yahoo は 1 step に全銘柄を入れず、step ごとに結果を durable に保存し、失敗しても最初からやり直さない。1 step の銘柄数は Hobby の Function の上限に収まる値を実測で決める。Vercel Cron（prediction 平日 16:10 JST、outcome 平日 18:00 JST）から起動し、JPX calendar の guard を workflow 側にも持つ。artifact は Vercel Blob（`surge/phase-b/<cohort_id>/<s0>/...`、write-once）、Canonical 本文と API key は複製しない。`TYPESAFE_API_KEY` は Vercel の環境変数に dashboard から入れる。段階: Stage 1 UI（fixture）→ 2 artifact storage（送信なし）→ 3 実データで selection まで（Jev に送らない、PC と比較）→ 4 Jev 1 件の smoke → 5 1 営業日の shadow。切替は universe・screening・Primary/Control・request hash・Jev schema・outcome 計算・artifact integrity の一致を確かめてからで、判断はユーザー。Windows task を勝手に無効化しない。Hobby 内で作り、有料プランは禁止、無料枠を超えそうなら実行前に止めて報告。**Claude Code の決定（可逆）**: CLAUDE.md §2「Web に重い全市場処理を載せない」はこの指示で shadow に限り上書きされたものとして扱う（Web のリクエスト処理には載せず、Workflows の step で動かす）／ artifact は PC の RunStore と同じ JSON 直列化にし、同じ中身は同じ SHA-256 にする／ request body は保存せず hash で参照する／ Web は list を使わず key を導出し、見つかった object は永久キャッシュ（Blob の advanced operations 2,000/月を守るため）／ Jev の確率は「Jev 自身の回答、未校正」と明記して表示する（CLAUDE.md 1-17）／ 既存の Dashboard は `/dashboard` へ移す。**調査で分かった制約**: Python Workflow SDK（`vercel-workflow` 0.10.2）と Services はどちらも beta で、組み合わせの公式例はない（Stage 3 で canary）／ Hobby の Blob は advanced operations 2,000/月・超過で 30 日停止・チーム共有（既存 store 1 つ）／ Workflow events 50,000/月・data written 1 GB/月／ Cron は 1 日 1 回・±59 分／ Function 300 s。**状態（2026-09-19）**: Stage 1・2 を実装（Web の 6 画面、Blob の write-once store、artifact 形式と integrity、PC からの export、synthetic fixture、CI の画面 smoke）。Vercel project と Blob store は未作成（ユーザー確認待ち）、Jev は呼んでいない。**ユーザー承認（2026-09-19）後**: project `surge-research-platform`・private Blob store `surge-shadow` を作成、Vercel Authentication = All Deployments、Stage 1 を production にデプロイし未認証は遮断・OIDC の認証済みアクセスで 6 画面を確認、Services（Next.js + Python）で Python Workflow SDK が動くこと、Cron → 保護された production → Workflow → 完了（シークレットなし）、Stage 2 をクラウドで（合成 28 object の write-once 書き込み・integrity 照合・上書き拒否）を確認。shadow の TypeSafe hard budget は **$0.30**、PC の $2.50 とは別 ledger（ユーザー決定）。詳細は [vercel-shadow.md](specs/vercel-shadow.md)。**Stage 3 実装（2026-09-19）**: PC の `plan_day` / `build` を step に分けた workflow（判定・選定・request の組み立ては凍結コードの関数をそのまま呼ぶ）と、PC と同じ合成データでの byte 比較テスト。Vercel 上の実行は Blob 使用量のダッシュボード確認後。PC と同じ S0 での比較は `select_day` の凍結規則により S0 = 2026-09-24 から。 | 2026-09-19 / ユーザー | [vercel-shadow.md](specs/vercel-shadow.md) |
| D-260 | **Groq の Entry request 413 は blocker として扱い、調査を打ち切る** | 測定結果は D-256 と research doc に保存済み。readiness では `analysis_request_accepted` / `analysis_live_smoke_passed` / `analysis_output_mode_usable` の FAIL として現れる。二分探索・追加実験はしない（ユーザー指示）。解除はモデル・プランの判断（D-11） | 2026-09-18 / ユーザー指示 | 同上 |

---

## B. 未決（Remaining）

### B-1. Phase 1 の前に必要

| ID | 種別 | 論点 | Claude Code の意見 |
|---|---|---|---|
| ~~D-00（登録）~~ | 前提 | **解決済み（A 表 D-00 参照）**。以下は経緯の記録: v5.1 全文の受領と Canonical 登録（Phase 1 の唯一の開始条件）。2026-09-16 の監査は「直前にユーザーが提示した全文」を Canonical と確定したが、**その本文は Claude Code 側のセッションには渡っていない**（ChatGPT 側の会話に存在すると考えられる）。捜索済み: 本セッションの文脈、全 Claude Code セッション記録（`v5.1` / `3000円以下限定` / `何銘柄を実際に評価できたか` / `織り込み判定` / `マルチスクリーニング` / `3段階ファネル` / `Dynamic Driver Score` / `Bigdata.com`）、Downloads / Documents / Desktop / OneDrive 配下、リポジトリ内。Google Drive コネクタは未認可で検索不可 | 記憶からの再生成・近似復元は禁止されているため作成しない。受領方法は (a) チャットへ全文貼り付け (b) ローカルにファイル保存してパスを伝える（バイト列をそのまま保持できるためこちらが確実）。受領後ただちに `short-surge-v5.1.original.md` へ無加工保存し、SHA-256 を MANIFEST に登録して D-00 を RESOLVED にし、Phase 1 に着手する |

### B-2. Phase 1 中に決めればよい（pending で開始可）

| ID | 種別 | 論点 | Claude Code の意見 |
|---|---|---|---|
| D-07a | 費用 | 米国株の役割ごとの Provider（Phase 1 はマスタ用） | **調査完了・提案あり（ユーザー承認待ち）**: EODHD All World $19.99/月。根拠と対抗馬は [phase-2-provider-evaluation.md](research/phase-2-provider-evaluation.md) §2 |
| D-10a | 投資ロジック | 「適格 ADR」の定義 | 判断しない。確定まで ADR は pending |
| D-10b | 投資ロジック/技術 | JP の普通株判定（`ProdCat` の値）、東証上場の外国株式、出資証券・優先出資証券、`0109 その他` | Phase 1 の最初に値を確認して提示 |
| D-10c | 投資ロジック/技術 | 米国 REIT の判定方法 | — |
| D-10d | 投資ロジック | NYSE Arca / Cboe BZX / IEX にのみ上場する普通株 | 判断しない |
| D-10e | 投資ロジック | 売買停止・監理/整理・Nasdaq Financial Status 異常 | 判断しない |
| D-14 | 運用 | 認証方式、障害通知 | Supabase Auth + 本人メールの allowlist |
| D-16 | 運用 | プロジェクト名（仮 `surge-research-platform`） | 仮名のまま |

### B-3. Phase 2〜8 の前に必要

| ID | 種別 | 論点 | 必要な時期 | Claude Code の意見 |
|---|---|---|---|---|
| D-01a | 投資ロジック | `entry_price_method` | Phase 8 前 | 場中 Provider の能力を確認してから。**2026-09-18: Prediction 生成には不要になった**（D-261: 基準価格は確定終値）。将来の実売買検証（`execution_price` / `next_session_entry_price`）を設計するときに決める |
| **D-268** | **投資ロジック（Outcome 側）** | **+20% 到達を intraday high と close のどちらで判定するか（EOD Prediction の Outcome）。** 現仕様から確定できない（ユーザー、2026-09-18）。参考: 現行 teacher-labels 仕様の Objective `hit_20` は「各 horizon 内の comparable 高値が基準価格 ×1.20 以上」で定義されている。**EOD Prediction 生成の blocker ではない** | EOD Prediction の Outcome engine を作る前 | 判断しない |
| ~~D-272~~ | 評価設計 / 投資ロジック（候補生成） | **解決済み（A 表 D-276 参照、2026-09-19 ユーザー決定）**。以下は経緯: **Jev 評価 Phase B（prospective shadow）の候補量の扱い。** Phase A の point-in-time replay（run `jev-a-20260918-01/-02`、400 銘柄 × 36 日）で、現行 Routes A–H（route-1.0.0）は適格（S0 終値 ≤ 3,000 円）の **61.2%** を通した（日別 15.9〜76.5%、中央値 64.4%）。**Route D（volatility contraction）単独で適格の 35.0%**（D だけで通過したのが 7,169 件中 2,667 件）、B 18.4%・H 13.5%・A 13.0%。本番でも毎日の Primary 候補がこの規模になり得るので、Phase B の前に決める: (1) 1 日あたりの最大 shadow prediction 数、(2) 候補が上限を超えたときの sampling / selection の規則、(3) 候補生成への Route D の寄与をどう扱うか。**ユーザー指示（2026-09-18）: 評価実装の bug とは扱わない。Phase A は投資ロジック・route 閾値を変えずにそのまま進める** 。**2026-09-19 追記**: 判断材料の throughput は D-275 で確定（TypeSafe direct は 2 秒間隔で 6 / 6 成功、Phase B は 1 request / 5 秒で開始、月次 $5 の credit で約 5,000 件）。**Phase B は D-272 を決めるまで開始しない**（ユーザー指示）| Jev 評価 Phase B の前 | 判断しない（route 閾値は投資ロジック、CLAUDE.md 1-3） |
| ~~D-265~~ | 投資ロジック/技術 | **解決済み（A 表 D-267 参照）**。以下は経緯: **EOD Prediction（D-261）の具体規則。** 引け後の Prediction を実際に作るには次を決める必要がある: (1) +20% Target / Failure Line を確定終値から計算するか（v5.1 は「分析基準価格 × 1.20」）、(2) Horizon の起点（S0 = 基準の終値のセッション、値動きは S1 から？）、(3) 3,000 円 Hard Filter を確定終値で1回確認するか、(4) Stage 3（EOD 分析）が Prediction を作れるようにする変更（現在は「EOD は ENTRY を出さない」が code と DB で強制されている）、(5) `prod.predictions` の `entry_reference_price` / `entry_price_observed_at`（NOT NULL・判断より後の CHECK）をどう扱うか、(6) LLM 向け addendum の文言と CLAUDE.md 1-4 の改訂 | EOD Prediction の実装前 | Claude Code の案: (1) v5.1 どおり確定終値 × 1.20、(2) S0 = 終値のセッション（S0 には値動きが残っていない）・評価は S1 から、(3) 確定終値で確認、(4)〜(6) は (1)〜(3) の確定後に versioned migration と新 addendum で。場中 ENTRY の pipeline（Phase 8）は削除せず休止のまま残す |
| D-40 | 技術 | US の `PROVISIONAL` 証券 6,328 件（SEC `company_tickers_exchange.json` に CIK がない ETF・ワラント等）と `REGISTRY_ANCHORED` 6,909 件を、security-level の安定識別子（FIGI / share-class 識別子 / provider の安定 ID）で `STRONG` 化できるか。**Phase 2 の US Provider 選定時に取得可否を確認し、取得できた場合にのみ昇格する**（D-44） | Phase 2 の Provider 選定時 | **調査完了・提案あり**: FIGI（`shareClassFIGI` / `compositeFIGI`）はパブリックドメインで DB 保存も再配布も明示的に許諾されており、唯一採用できる share-class 識別子。CUSIP は規約が master DB 化を否定し $46,825/年、ISIN は US ISIN に埋め込まれた CUSIP の権利関係が未解決、SEC は証券レベルの識別子を発行していない。取得経路は OpenFIGI（無料）/ Massive `/v3/reference/tickers`（$0 プラン）/ EODHD ID Mapping（`filter[cik]` が使える）。**昇格は identity_version を伴う記録された migration として別途行う**。[評価記録](research/phase-2-provider-evaluation.md) §5 |
| D-41 | 技術 | JP の `PROVISIONAL` 発行体 732 件（EDINET コード一覧に載らない ETF・REIT・出資証券等）の扱い | Phase 2 前 | 発行体の統合が必要になるのは主に普通株。EDINET 未突合は UNRESOLVED/EXCLUDED 側に偏っている |
| D-42 | 投資ロジック/技術 | 同一 CIK に複数の証券がぶら下がる 640 CIK（クラス株・優先株・ワラント等）のうち、どこまでを「同一発行体の別クラス」として扱い、どこからを別発行体とみなすか | Phase 3 前 | 現状は CIK = 発行体、クラスは証券側で分離。例外（合併・持株会社化で CIK が変わる場合）の扱いは未定 |
| D-43 | 技術 | US の identity collision 240 レコード / **67 distinct キー**（同一 CIK・同一種別・同一クラスに複数銘柄。最大は `US:CIK:0000927971:ETF:` の 42 銘柄）を、どの追加属性で分離するか | Phase 3 前 | 現状は両方を PROVISIONAL に降格し、`context` 付きで警告に残す。大半は ETN / レバレッジ ETF。discriminator を広げると 240 件の `security_id` が変わるため、rebuild としてしか実施できない |
| D-70 | 規約/技術 | JP の5桁証券コードの公式定義（証券コード協議会の仕様書 PDF）を取得し、コード形ガードを「公式根拠あり」へ格上げするか | Phase 3 前（銘柄名が決定的なため blocker ではない） | 公式ファイルの実測（4桁=普通株 4,434件 / 5桁=特殊株式 7件）で代用している |
| ~~D-53~~ | 技術 | **解決済み（A 表 D-53 参照）**: publication model を実装 | — | — |
| D-01b | 投資ロジック | 「次の取引可能時点」の定義 | Phase 8 前 | 判断しない |
| D-02a | 技術/投資ロジック | FX の Provider と許容遅延 | Phase 2 前 | **調査完了・提案あり**: 一次 = ECB 参照レート（無料・訂正されない）、二次 = EODHD `USDJPY.FOREX`（US プランに同梱）。[評価記録](research/phase-2-provider-evaluation.md) §3。**場中 ENTRY 用のレート選択は未決**（ECB は日次1本） |
| D-03b | 費用/技術 | Object Storage プロバイダ | Phase 2 前 | **調査完了・提案あり**: Cloudflare R2（egress 無料・バケット単位の資格情報）。Versioning が無いため write-once キー運用と Bucket Lock で代替。日本リージョン保証が要るなら S3 ap-northeast-1。[評価記録](research/phase-2-provider-evaluation.md) §4 |
| D-03c | 費用 | Supabase の Production プラン（D-03a の再評価） | Production 開始前 | — |
| D-05a | 費用/技術 | Production の Runner・Scheduler | Phase 4・8 前 | 場中とニュース収集は常駐型が必要な見込み |
| D-06a | 費用 | J-Quants の Production プランと分足・ティックアドオン | Phase 2 前 | **調査完了・提案あり**: EOD のみなら Standard ¥3,300/月（日足10年）。Light ¥1,650 は5年。配当は Premium ¥16,500 限定だが、1-9 により配当は +20% Target に加算しないため Phase 2 では不要。**ダウングレードすると上位プランでしか取れなかったデータの削除義務が生じる**点に注意。[評価記録](research/phase-2-provider-evaluation.md) §1 |
| **D-76** | **規約** | **J-Quants の利用条件（本人の私的利用に限る／法人不可／学術不可／解約・ダウングレード時の削除義務）を本プロジェクトが恒久的に満たし続けるかの確認** | **Phase 2 の取得開始前（blocker）** | Claude Code は判断しない。現状（個人1名の私的研究、GitHub には**コードのみ**公開、生データは Private）は規約の範囲内に読めるが、閲覧者が増える・法人化する・分析結果を継続反復的に公開する場合は範囲外になる |
| **D-77** | **技術/投資ロジック** | **US EOD の出来高が分割調整済みであることへの対処**（EODHD は OHLC が raw、Volume のみ split 調整済み）。splits フィードから raw 株数を復元するか、`volume_adjustment_state` を列として持って調整済みのまま扱うか | Phase 2 のスキーマ確定前 | 1-9 は「raw（取引された無調整価格）を保存」と定めており、出来高もその対象と読むのが自然。復元は分割履歴が完全な期間に限られる（EODHD は2018年より前の廃止銘柄の splits を持たない） |
| **D-78** | **技術** | **EOD 公表時刻が公式に明示されない Provider（EODHD の US EOD、EODHD FX）について、`available_at` をどう決めるか** | Phase 2 の取得実装前 | 自分の取得時刻（`received_at`）を `available_at` とし、Provider の公表時刻を推定値として混ぜない。1-7 の「`source_published_at` を利用可能時刻の代わりにしない」と同じ原則 |
| **D-92** | **規約/技術** | **EODHD `/api/id-mapping` のレスポンス形**。散文ページは `{meta, data[{symbol,isin,figi,lei,cusip,cik}], links}`、公式 OpenAPI は裸の配列 `[{Code,Exchange,Name,ISIN,FIGI,LEI,CUSIP,CIK}]` と**両立しない**。実レスポンスで決着させるまで使わない | 使用する前（Phase 2.2 以降） | どちらでもない側に実装すると全列が黙って null になる。CUSIP を返す点でも CGS の間接エンドユーザー条項に触れるため、取り込み時に cusip/isin を破棄する運用が要る |
| **D-93** | **技術** | **J-Quants `ProdCat` と Phase 1 の銘柄名判定の突き合わせ結果をどう扱うか**（不一致銘柄を UNRESOLVED にするか、ProdCat を優先するか） | Phase 2.2 の JP universe 再構築前 | Claude Code は判断しない。ProdCat は普通株と優先株式を区別しないため、単純な置き換えはできない |
| **D-102** | **規約（blocker）** | **JPX 統計ファイルの「二次利用」の解釈。** 公表された統計ファイルとその値を、本人だけが閲覧する私的DBに保持し自分の投資判断にのみ使うことが二次利用に当たるか。**JP の当日EOD価格を0円で得る唯一の経路**で、可否がここで決まる | JP 価格取得の着手前 | 東京証券取引所 株式部データサービス室へ書面照会。robots.txt は全許可、規約は二次利用を未定義のまま禁止しており、公式文書だけでは決着しない |
| **D-103-EOD** | **規約/技術** | **US の consolidated 日次 OHLCV を 0 円で保存できるか。** adapter は実装済み（delayed SIP、`feed=sip` 明示、15分窓の検査、`venue_basis`）。残るのは (a) credential、(b) **保存条項が `NOT_SPECIFIED`**（D-178） | historical ingest の開始前 | (b) の結論が出るまで `assert_smoke_sized` の上限内でしか取得しない。role binding は `enabled = false` |
| **D-103-LIVE** | **規約/技術** | **US の「判断時点で取引可能だった価格」を 0 円で取得できるか。未解決。** Alpaca Basic の real-time は IEX のみで、これは単一取引所（D-173）。15分遅れの historical は `entry_reference_price` になり得ない | US の formal Prediction 開始前 | Outcome 用と Entry 用が同一 provider である必要は無い。D-103-EOD が解けても、これは解けていない。**2026-09-18: Prediction 生成の blocker ではなくなった**（D-261: 基準価格は確定終値。US は Alpaca SIP 日足、D-262）。将来の実売買検証を設計するときに再開 |
| **D-104** | **規約** | （実装側は完了。Local Production Runner・CLI・Windows Task Scheduler の install / uninstall / status / dry-run / heartbeat script を作成済み。残るのは**タスク登録そのもの**で、これはユーザーの機械の設定変更にあたる）<br>**GitHub Actions を日次取得の実行環境にしない判断の確認。** Actions 追加規約は GitHub-hosted runner の用途を当該リポジトリのソフトウェアの production/testing/deployment/publication に限定し、違反時の措置はリポジトリ無効化・アカウント停止 | 日次運用の開始前 | Claude Code の判断: **使わない**。代替はローカル実行 / Oracle Always Free (A1 2 OCPU・回収リスクあり) / GitHub への書面確認。CI（テスト）としての利用は規約どおりの用途で問題ない |
| **D-105** | **費用/技術** | **Cloudflare R2 のカード登録。** バケット作成前に subscription checkout が必須で、超過は自動停止せず課金される。支払い失敗時はバケット利用不可、30日でデータ削除の可能性 | R2 バケット作成前 | 無料枠内に留めるのは**運用ポリシー**であって Cloudflare は止めてくれない。lifecycle と保持期間で 10 GB-月を守る設計が前提 |
| **D-06b** | 費用/技術 | **日本株の場中 ENTRY 判断・Watch 監視用リアルタイム Provider** | **Phase 8 までの blocker（Phase 1 の blocker ではない）** | 公式情報のみで候補を調査。2026-09-18 追加調査: 無料で「東証 + 場中 event timestamp + 保存が禁止されていない」を満たすものは見つからず（[jp-intraday-free-sources-2026-09-18.md](research/jp-intraday-free-sources-2026-09-18.md)、再検証パス未実施）。**同日、Prediction 生成の blocker ではなくなった**: 基準価格は確定終値（D-261）で、JP は Yahoo 日足（D-262、ユーザーの screener のロジックを再利用）。場中の Watch 監視・`decision_price` 用の場中 source が要るかは D-265 次第 |
| ~~D-257~~ | 規約（CLAUDE.md 1-1） | **解決済み（A 表 D-258 参照）**。2026-09-18、ユーザーが個人の投資研究用途として `mea-stock-screener` の読み取りと価格取得ロジックの再利用を**今回限りの 1-1 の例外として明示許可** | — | — |
| D-08a | 投資ロジック | Stage 2 で取得する分足の期間 | Phase 6 前 | v5.1 を確認してから |
| D-11 | 費用/技術 | LLM プロバイダ・モデル・月額上限 | Phase 5〜7 前 | 2026-09-18: Groq Free tier では Entry request が 413（`groq/compound`・`openai/gpt-oss-120b`、D-256）。**blocker として保留**（D-260）。有料化で解消するとは記録していない。直接の llama-4-scout はこのアカウントで利用不可（404、D-266）→ **無料 Groq は打ち切り、別の無料モデル / provider を検討** |
| D-12 | 費用/規約 | ニュース・開示ソースの取得手段 | Phase 4 | 規約調査表を提出 |
| D-17a | 投資ロジック | `thesis_key`（同一仮説）の定義 | Phase 8 前 | 判断しない |
| D-17b | 投資ロジック | Open Episode 中の別仮説、クローズ後の再 ENTRY | Phase 8 前 | 判断しない |
| D-20 | 投資ロジック | Setup（両種別）の有効期間 | Phase 8 前 | 判断しない |
| D-21 | 投資ロジック | 15分遅延データを `decision_price` / `entry_reference_price` に使うことを許すか | Phase 8 前 | 許す場合も `latency_class` で区別。2026-09-18: D-261 の下では Prediction は引け後に**確定終値**を使うので、遅延は確定を待つ時間（D-262）に吸収され、この論点は Prediction 生成では生じない。場中の判断に遅延データを使うかは、場中 pipeline を再開する場合にだけ残る |
| D-31 | 投資ロジック | ENTRY と判断したが `entry_reference_price` を観測できなかった場合（Provider 障害・約定なし等）の `entry_attempts.status` と扱い | Phase 8 前 | 判断しない。少なくとも Prediction は作らず理由を記録すべき |
| D-32 | 投資ロジック | `price_obstacles.status` を WEAKENED / INVALIDATED にする基準（新材料・出来高・価格受容・高値突破の条件） | Phase 7 前 | 判断しない。v5.1 の記述を確認してから |

### B-4. Phase 9 以降

| ID | 種別 | 論点 | 必要な時期 |
|---|---|---|---|
| D-04 | 費用/運用 | Vercel のプラン | Web 公開時 |
| D-13a | 投資ロジック | 12ラベルの分類と判定基準、「合理的に拾えた」の基準 | Phase 10 前 |
| D-13b | 投資ロジック | `label_admission_policy` の内容 | Phase 10 前 |
| D-15 | 運用 | Excel の受け取り方法 | Phase 9 |
| D-24 | 投資ロジック | 分割・併合・配当以外の corporate action の Outcome での扱い | Phase 9 前 |
| D-33 | 投資ロジック | そもそも収集対象にしていなかったソースの情報を `PIPELINE_MISSED_ACTIONABLE_SIGNAL` に含めるか（監査は「Collector 障害・取得遅延等」と記載） | Phase 10 前 |
